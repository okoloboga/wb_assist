# Интеграция скрапинга конкурентов Wildberries в проект

## 🎯 Обзор

Данный документ описывает интеграцию скрипта скрапинга данных о конкурентах Wildberries в рабочий процесс бота. Скрапинг реализован как часть backend-сервиса (server) и запускается через Celery задачи.

## 📋 Требования и ограничения

### Формат ссылок
- Принимаются ссылки на страницы **брендов** или **селлеров** (seller)
- Примеры:
  - `https://www.wildberries.ru/brands/310770244-meromi`
  - `https://www.wildberries.ru/seller/123456`
- Обработка для обоих типов ссылок идентична

### Лимиты (настраиваются через .env)
- **Максимум конкурентов на кабинет**: 10 (по умолчанию)
  - Переменная: `COMPETITOR_MAX_LINKS_PER_CABINET=10`
- **Максимум товаров с одной ссылки**: 30 (по умолчанию)
  - Переменная: `COMPETITOR_MAX_PRODUCTS_PER_LINK=30`

### Проверка дублирования
- Нельзя добавить одного и того же конкурента дважды в один кабинет
- Проверка по полному URL ссылки

## 🏗️ Архитектура интеграции

### Компоненты системы

```
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│  Telegram Bot   │───►│  Backend Server  │───►│   PostgreSQL    │
│   (Aiogram)     │    │    (FastAPI)     │    │                 │
└─────────────────┘    └──────────────────┘    └─────────────────┘
                              │
                              │ Celery Task
                              ▼
                       ┌──────────────────┐
                       │  Scraping Module │
                       │   (Selenium)     │
                       └──────────────────┘
```

### Поток данных

1. **Пользователь добавляет ссылку конкурента**:
   - В меню "Конкуренты" пользователь отправляет ссылку в чат
   - Бот валидирует ссылку и проверяет лимиты
   - Ссылка сохраняется в БД со статусом "pending"

2. **Запуск скрапинга**:
   - Celery задача запускается асинхронно
   - Используется Selenium с Chrome в headless режиме
   - Собираются данные: название, цена, бренд, категория, рейтинг, описание

3. **Сохранение данных**:
   - Данные сохраняются в БД с иерархией: Бренд → Категория → Товары
   - Название бренда/магазина извлекается из данных и используется для кнопок

4. **Отображение в боте**:
   - Кнопка конкурента появляется только после успешного завершения скрапинга
   - При нажатии на конкурента показывается список товаров с пагинацией (10 на страницу)
   - При нажатии на товар показываются детали

5. **Автообновление**:
   - Раз в сутки запускается обновление данных по всем конкурентам
   - Время обновления распределяется по `cabinet_id` для равномерной нагрузки
   - Все товары перезаписываются (цены могут меняться)

## 🗄️ Структура базы данных

### Таблица `competitor_links`
```sql
CREATE TABLE competitor_links (
    id SERIAL PRIMARY KEY,
    cabinet_id INTEGER REFERENCES wb_cabinets(id) ON DELETE CASCADE,
    competitor_url TEXT NOT NULL,
    competitor_name VARCHAR(255),  -- Извлекается из данных скрапинга
    status VARCHAR(20) NOT NULL DEFAULT 'pending',  -- pending, scraping, completed, error
    products_count INTEGER DEFAULT 0,
    last_scraped_at TIMESTAMP,
    next_update_at TIMESTAMP,  -- Время следующего обновления (распределение по cabinet_id)
    error_message TEXT,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    UNIQUE (cabinet_id, competitor_url)  -- Защита от дублирования
);

CREATE INDEX idx_competitor_cabinet ON competitor_links(cabinet_id);
CREATE INDEX idx_competitor_status ON competitor_links(status);
CREATE INDEX idx_competitor_next_update ON competitor_links(next_update_at);
```

### Таблица `competitor_products`
```sql
CREATE TABLE competitor_products (
    id SERIAL PRIMARY KEY,
    competitor_link_id INTEGER REFERENCES competitor_links(id) ON DELETE CASCADE,
    nm_id VARCHAR(50) NOT NULL,  -- Артикул товара WB
    product_url TEXT NOT NULL,
    name TEXT,
    current_price DECIMAL(10,2),
    original_price DECIMAL(10,2),
    brand VARCHAR(255),
    category VARCHAR(255),
    rating DECIMAL(3,2),
    description TEXT,
    scraped_at TIMESTAMP DEFAULT NOW(),
    UNIQUE (competitor_link_id, nm_id)
);

CREATE INDEX idx_competitor_product_link ON competitor_products(competitor_link_id);
CREATE INDEX idx_competitor_product_nm ON competitor_products(nm_id);
```

## ⚙️ Переменные окружения

Добавить в `.env` и `docker-compose.yml`:

```bash
# Лимиты скрапинга конкурентов
COMPETITOR_MAX_LINKS_PER_CABINET=10
COMPETITOR_MAX_PRODUCTS_PER_LINK=30

# Настройки автообновления
COMPETITOR_UPDATE_INTERVAL_HOURS=24  # Обновление раз в сутки
COMPETITOR_UPDATE_SPREAD_HOURS=2     # Разброс времени для распределения нагрузки
```

В `docker-compose.yml` добавить в секцию `server` и `celery-worker`:

```yaml
environment:
  # ... существующие переменные ...
  - COMPETITOR_MAX_LINKS_PER_CABINET=${COMPETITOR_MAX_LINKS_PER_CABINET:-10}
  - COMPETITOR_MAX_PRODUCTS_PER_LINK=${COMPETITOR_MAX_PRODUCTS_PER_LINK:-30}
  - COMPETITOR_UPDATE_INTERVAL_HOURS=${COMPETITOR_UPDATE_INTERVAL_HOURS:-24}
  - COMPETITOR_UPDATE_SPREAD_HOURS=${COMPETITOR_UPDATE_SPREAD_HOURS:-2}
```

## 🔧 Реализация компонентов

### 1. Backend: Модели и CRUD

**Файл**: `server/app/features/competitors/models.py`
- `CompetitorLink` - модель для ссылок конкурентов
- `CompetitorProduct` - модель для товаров конкурентов

**Файл**: `server/app/features/competitors/crud.py`
- CRUD операции для работы с конкурентами и товарами
- Проверка лимитов и дублирования

### 2. Backend: Celery задачи

**Файл**: `server/app/features/competitors/tasks.py`

```python
@celery_app.task(bind=True, max_retries=3)
def scrape_competitor_task(self, competitor_link_id: int):
    """
    Задача скрапинга данных конкурента
    - Получает URL из БД
    - Запускает Selenium скрапинг
    - Сохраняет данные в БД
    - Обновляет статус и название конкурента
    """
    pass

@celery_app.task(bind=True)
def update_all_competitors_task(self):
    """
    Периодическая задача обновления всех конкурентов
    - Запускается раз в сутки через Celery Beat
    - Распределяет время обновления по cabinet_id
    - Запускает scrape_competitor_task для каждого конкурента
    """
    pass
```

**Добавить в `server/app/core/celery_app.py`**:
```python
include=[
    # ... существующие ...
    "app.features.competitors.tasks",
]

beat_schedule={
    # ... существующие ...
    "update-all-competitors": {
        "task": "app.features.competitors.tasks.update_all_competitors_task",
        "schedule": crontab(hour=0, minute=0),  # Каждый день в полночь
    },
}
```

### 3. Backend: API эндпоинты

**Файл**: `server/app/features/competitors/routes.py`

```python
@router.post("/competitors/add")
async def add_competitor(
    telegram_id: int,
    competitor_url: str,
    db: Session = Depends(get_db)
):
    """
    Добавить ссылку конкурента
    - Валидация URL
    - Проверка лимитов
    - Проверка дублирования
    - Сохранение в БД
    - Запуск Celery задачи скрапинга
    """

@router.get("/competitors")
async def get_competitors(
    telegram_id: int,
    offset: int = 0,
    limit: int = 10,
    db: Session = Depends(get_db)
):
    """
    Получить список конкурентов с пагинацией
    - Только завершенные скрапингом (status='completed')
    - Пагинация: 10 на страницу
    """

@router.get("/competitors/{competitor_id}/products")
async def get_competitor_products(
    competitor_id: int,
    offset: int = 0,
    limit: int = 10,
    db: Session = Depends(get_db)
):
    """
    Получить товары конкурента с пагинацией
    - Пагинация: 10 на страницу
    """
```

### 4. Backend: Модуль скрапинга

**Файл**: `server/app/features/competitors/scraper.py`

Адаптировать логику из `scrapping/test.py`:
- `get_driver()` - создание Selenium WebDriver с headless режимом
- `get_product_urls()` - получение списка URL товаров со страницы бренда/селлера
- `scrape_product_data()` - сбор данных по одному товару
- `scrape_competitor()` - основная функция скрапинга

**Зависимости для `server/requirements.txt`**:
```txt
selenium>=4.0.0
webdriver-manager>=4.0.0
beautifulsoup4>=4.12.0
```

**Обновить `server/Dockerfile`** для поддержки Selenium:
```dockerfile
FROM python:3.12

WORKDIR /app

# Устанавливаем системные зависимости для Chrome и Selenium
RUN apt-get update && apt-get install -y \
    wget \
    unzip \
    xvfb \
    chromium \
    chromium-driver \
    --no-install-recommends && \
    rm -rf /var/lib/apt/lists/*

# Копируем и устанавливаем Python зависимости
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Копируем приложение
COPY . .

EXPOSE 8000

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 5. Bot: Обработчики

**Файл**: `bot/handlers/competitors.py` (новый)

```python
@router.callback_query(F.data == "competitors")
async def show_competitors_menu(callback: CallbackQuery):
    """Показать меню конкурентов с пагинацией"""
    # Получить список конкурентов через API
    # Отобразить кнопки с пагинацией (10 на страницу)

@router.message(F.text.startswith("https://www.wildberries.ru/"))
async def handle_competitor_url(message: Message):
    """Обработка ссылки конкурента от пользователя"""
    # Валидация URL
    # Отправка на backend API
    # Показать статус "Скрапинг запущен, кнопка появится после завершения"

@router.callback_query(F.data.startswith("competitor_"))
async def show_competitor_products(callback: CallbackQuery):
    """Показать товары конкурента с пагинацией"""
    # Получить товары через API
    # Отобразить с пагинацией (10 на страницу)
```

**Обновить `bot/keyboards/keyboards.py`**:
- Добавить кнопку "Конкуренты" в `wb_menu_keyboard()`
- Создать функции для динамических клавиатур конкурентов и товаров

**Обновить `bot/core/states.py`**:
```python
class CompetitorStates(StatesGroup):
    """Состояния для работы с конкурентами"""
    waiting_for_url = State()
```

### 6. Bot: API клиент

**Обновить `bot/api/client.py`**:
```python
async def add_competitor(
    self,
    user_id: int,
    competitor_url: str
) -> BotAPIResponse:
    """Добавить ссылку конкурента"""

async def get_competitors(
    self,
    user_id: int,
    offset: int = 0,
    limit: int = 10
) -> BotAPIResponse:
    """Получить список конкурентов"""

async def get_competitor_products(
    self,
    competitor_id: int,
    offset: int = 0,
    limit: int = 10
) -> BotAPIResponse:
    """Получить товары конкурента"""
```

## 📊 Распределение времени обновления

Для равномерной нагрузки на WB используется распределение времени обновления по `cabinet_id`:

```python
def calculate_next_update_time(cabinet_id: int, base_hour: int = 0) -> datetime:
    """
    Вычисляет время следующего обновления на основе cabinet_id
    Распределяет обновления в течение дня для разных кабинетов
    """
    import hashlib
    
    # Используем хеш cabinet_id для получения смещения
    hash_value = int(hashlib.md5(str(cabinet_id).encode()).hexdigest(), 16)
    spread_hours = int(os.getenv("COMPETITOR_UPDATE_SPREAD_HOURS", "2"))
    
    # Смещение в часах от базового времени
    offset_hours = hash_value % spread_hours
    
    next_update = datetime.now().replace(hour=base_hour, minute=0, second=0)
    next_update += timedelta(hours=offset_hours, days=1)
    
    return next_update
```

## 🔄 Процесс добавления конкурента

1. Пользователь открывает меню "Конкуренты" в боте
2. Отправляет ссылку на бренд/селлера в чат
3. Бот валидирует URL и проверяет:
   - Формат ссылки (wildberries.ru/brands/ или /seller/)
   - Лимит на количество конкурентов (10)
   - Отсутствие дублирования
4. Если валидация успешна:
   - Ссылка сохраняется в БД со статусом "pending"
   - Запускается Celery задача `scrape_competitor_task`
   - Пользователю показывается: "Скрапинг запущен, кнопка появится после завершения"
5. Celery задача:
   - Меняет статус на "scraping"
   - Запускает Selenium скрапинг
   - Собирает до 30 товаров
   - Сохраняет данные в БД
   - Извлекает название бренда/магазина
   - Меняет статус на "completed" или "error"
6. После завершения кнопка конкурента появляется в меню

## 📝 Особенности реализации

### Обработка ошибок
- При ошибке скрапинга статус меняется на "error", сохраняется `error_message`
- Retry логика в Celery: до 3 попыток с экспоненциальной задержкой
- При критических ошибках (блокировка WB, таймауты) задача помечается как failed

### Производительность
- Selenium запускается в headless режиме с Xvfb
- Отключена загрузка изображений для ускорения
- Таймауты: 30 сек на загрузку страницы, 20 сек на ожидание элементов
- Batch сохранение: каждые 5 товаров

### Безопасность
- Проверка дублирования на уровне БД (UNIQUE constraint)
- Валидация URL перед сохранением
- Ограничение лимитов через env переменные
- Изоляция скрапинга в Celery worker

## 🚀 Развертывание

1. **Обновить зависимости**:
   ```bash
   # В server/requirements.txt добавить selenium, webdriver-manager, beautifulsoup4
   ```

2. **Обновить Dockerfile сервера** для поддержки Chrome/Selenium

3. **Добавить переменные окружения** в `.env` и `docker-compose.yml`

4. **Создать миграции Alembic** для новых таблиц

5. **Пересобрать и перезапустить**:
   ```bash
   docker-compose down
   docker-compose build server celery-worker
   docker-compose up -d
   ```

## ✅ Критерии приёмки

- [ ] Пользователь может добавить ссылку конкурента через бота
- [ ] Проверка лимитов работает (максимум 10 конкурентов)
- [ ] Проверка дублирования работает
- [ ] Скрапинг запускается асинхронно через Celery
- [ ] Кнопка конкурента появляется только после успешного скрапинга
- [ ] Название конкурента извлекается из данных и отображается на кнопке
- [ ] Пагинация работает: 10 конкурентов и 10 товаров на страницу
- [ ] Автообновление запускается раз в сутки
- [ ] Время обновления распределяется по cabinet_id
- [ ] Все товары перезаписываются при обновлении
- [ ] Обработка ошибок и retry логика работают корректно

---

**Статус**: 📝 ТЗ готово к реализации  
**Приоритет**: Высокий  
**Связанные документы**: `docs/stages/S5/S5.md`, `docs/stages/S5/SCRAP.md`