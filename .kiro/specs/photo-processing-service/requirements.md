# Requirements Document

## Introduction

Данный документ описывает требования к сервису обработки фотографий через нейронную сеть. Система позволяет пользователям Telegram-бота загружать фотографии и получать обработанные изображения на основе текстовых описаний (промптов). Результаты обработки сохраняются в базе данных для последующего доступа.

## Glossary

- **Telegram Bot** - Telegram-бот, который взаимодействует с пользователями
- **GPT Service** - Сервис интеграции с нейронными сетями (порт 9000)
- **Image Generation API** - Внешний API для генерации/обработки изображений
- **FSM (Finite State Machine)** - Машина состояний для управления диалогом с пользователем
- **Photo Processing System** - Система обработки фотографий
- **User** - Пользователь Telegram-бота
- **Processing Result** - Результат обработки фотографии (URL обработанного изображения)
- **Prompt** - Текстовое описание желаемого результата обработки

## Requirements

### Requirement 1: Инициация процесса обработки фотографии

**User Story:** Как пользователь, я хочу начать процесс обработки фотографии через кнопку в меню, чтобы легко получить доступ к функционалу.

#### Acceptance Criteria

1. WHEN User нажимает кнопку "📸 Обработка фото" в меню AI помощника, THE Telegram Bot SHALL установить состояние FSM в "waiting_for_photo"
2. WHEN User нажимает кнопку "📸 Обработка фото", THE Telegram Bot SHALL отправить сообщение с инструкцией "Пришлите фото, которое нужно обработать"
3. WHEN User нажимает кнопку "📸 Обработка фото", THE Telegram Bot SHALL отобразить клавиатуру с кнопкой "🔙 Отмена"
4. THE Telegram Bot SHALL добавить кнопку "📸 Обработка фото" в клавиатуру AI помощника рядом с кнопкой "🎨 Генерация карточки"

### Requirement 2: Получение и сохранение фотографии

**User Story:** Как пользователь, я хочу отправить фотографию боту, чтобы она была обработана согласно моему описанию.

#### Acceptance Criteria

1. WHILE FSM находится в состоянии "waiting_for_photo", WHEN User отправляет фотографию, THE Telegram Bot SHALL сохранить file_id фотографии наилучшего качества в FSM
2. WHEN Telegram Bot получает фотографию от User, THE Telegram Bot SHALL перевести FSM в состояние "waiting_for_prompt"
3. WHEN Telegram Bot сохраняет фотографию, THE Telegram Bot SHALL отправить сообщение "✅ Фото получено! Опишите, что нужно сделать с фотографией"
4. WHILE FSM находится в состоянии "waiting_for_photo", WHEN User отправляет не фотографию, THE Telegram Bot SHALL отправить сообщение об ошибке "Пожалуйста, отправьте фотографию"

### Requirement 3: Получение и валидация промпта

**User Story:** Как пользователь, я хочу описать желаемый результат обработки текстом, чтобы получить изображение согласно моим требованиям.

#### Acceptance Criteria

1. WHILE FSM находится в состоянии "waiting_for_prompt", WHEN User отправляет текстовое сообщение, THE Telegram Bot SHALL сохранить текст промпта в FSM
2. WHEN Telegram Bot получает промпт от User, THE Telegram Bot SHALL валидировать длину промпта (минимум 3 символа, максимум 1000 символов)
3. IF промпт не проходит валидацию, THEN THE Telegram Bot SHALL отправить сообщение об ошибке с указанием требований к промпту
4. WHEN Telegram Bot получает валидный промпт, THE Telegram Bot SHALL отправить сообщение "⏳ Обрабатываю фотографию..."

### Requirement 4: Обработка фотографии через API

**User Story:** Как система, я хочу отправить фотографию и промпт в Image Generation API, чтобы получить обработанное изображение.

#### Acceptance Criteria

1. WHEN Telegram Bot получает валидный промпт, THE GPT Service SHALL загрузить фотографию из Telegram по file_id
2. WHEN GPT Service загружает фотографию, THE GPT Service SHALL отправить запрос в Image Generation API с фотографией и промптом
3. THE GPT Service SHALL установить таймаут запроса к Image Generation API равным 60 секундам
4. WHEN Image Generation API возвращает обработанное изображение, THE GPT Service SHALL получить URL обработанного изображения
5. IF Image Generation API возвращает ошибку, THEN THE GPT Service SHALL повторить запрос максимум 3 раза с экспоненциальной задержкой

### Requirement 5: Отправка результата пользователю

**User Story:** Как пользователь, я хочу получить обработанное изображение в Telegram, чтобы увидеть результат обработки.

#### Acceptance Criteria

1. WHEN GPT Service получает URL обработанного изображения, THE Telegram Bot SHALL отправить обработанное изображение User
2. WHEN Telegram Bot отправляет обработанное изображение, THE Telegram Bot SHALL отправить сообщение "✅ Фото обработано! Результат сохранен в вашей галерее"
3. WHEN Telegram Bot отправляет результат, THE Telegram Bot SHALL очистить состояние FSM
4. WHEN Telegram Bot отправляет результат, THE Telegram Bot SHALL вернуть User в главное меню

### Requirement 6: Сохранение результатов в базе данных

**User Story:** Как система, я хочу сохранять результаты обработки в базе данных, чтобы пользователи могли получить доступ к истории обработок.

#### Acceptance Criteria

1. WHEN GPT Service получает URL обработанного изображения, THE Photo Processing System SHALL сохранить запись в таблицу photo_processing_results
2. THE Photo Processing System SHALL сохранить в записи следующие поля: telegram_id, original_photo_file_id, prompt, result_photo_url, processing_service, processing_time, created_at
3. THE Photo Processing System SHALL создать индекс по полю telegram_id для быстрого поиска
4. THE Photo Processing System SHALL создать индекс по полю created_at для сортировки по дате
5. IF сохранение в базу данных завершается ошибкой, THEN THE Photo Processing System SHALL логировать ошибку и продолжить отправку результата User

### Requirement 7: Получение истории обработок

**User Story:** Как пользователь, я хочу получить доступ к истории моих обработанных фотографий, чтобы просмотреть предыдущие результаты.

#### Acceptance Criteria

1. WHEN система получает запрос истории для telegram_id, THE GPT Service SHALL вернуть список записей из таблицы photo_processing_results отфильтрованных по telegram_id
2. THE GPT Service SHALL поддерживать пагинацию с параметрами limit (по умолчанию 20) и offset (по умолчанию 0)
3. THE GPT Service SHALL сортировать записи по полю created_at в порядке убывания (новые первыми)
4. THE GPT Service SHALL возвращать для каждой записи поля: id, original_photo_file_id, prompt, result_photo_url, created_at, processing_time

### Requirement 8: Отмена процесса обработки

**User Story:** Как пользователь, я хочу отменить процесс обработки фотографии на любом этапе, чтобы вернуться в главное меню.

#### Acceptance Criteria

1. WHEN User нажимает кнопку "🔙 Отмена" во время процесса обработки, THE Telegram Bot SHALL очистить состояние FSM
2. WHEN User нажимает кнопку "🔙 Отмена", THE Telegram Bot SHALL отправить сообщение "Процесс обработки отменен"
3. WHEN User нажимает кнопку "🔙 Отмена", THE Telegram Bot SHALL вернуть User в главное меню
4. WHEN User отправляет команду /start во время процесса обработки, THE Telegram Bot SHALL очистить состояние FSM и перенаправить на стандартную регистрацию

### Requirement 9: Обработка ошибок API

**User Story:** Как пользователь, я хочу получать понятные сообщения об ошибках, чтобы понимать, что пошло не так.

#### Acceptance Criteria

1. IF Image Generation API возвращает HTTP статус 400, THEN THE Telegram Bot SHALL отправить User сообщение "Некорректные данные. Попробуйте другое фото или описание"
2. IF Image Generation API возвращает HTTP статус 403, THEN THE Telegram Bot SHALL отправить User сообщение "Ошибка доступа к сервису. Попробуйте позже"
3. IF Image Generation API возвращает HTTP статус 500 или 503, THEN THE Telegram Bot SHALL отправить User сообщение "Сервис временно недоступен. Попробуйте позже"
4. IF запрос к Image Generation API превышает таймаут 60 секунд, THEN THE Telegram Bot SHALL отправить User сообщение "Превышено время ожидания. Попробуйте еще раз"
5. IF все 3 попытки запроса к Image Generation API завершаются ошибкой, THEN THE Telegram Bot SHALL отправить User сообщение "Не удалось обработать фотографию. Попробуйте позже"

### Requirement 10: API эндпоинты GPT Service

**User Story:** Как Telegram Bot, я хочу взаимодействовать с GPT Service через REST API, чтобы обрабатывать фотографии и получать историю.

#### Acceptance Criteria

1. THE GPT Service SHALL предоставить эндпоинт POST /v1/photo/process для обработки фотографий
2. WHEN запрос отправляется на POST /v1/photo/process, THE GPT Service SHALL требовать заголовок X-API-KEY для аутентификации
3. WHEN запрос отправляется на POST /v1/photo/process, THE GPT Service SHALL принимать параметры: telegram_id, photo_file_id, prompt
4. WHEN обработка завершается успешно, THE GPT Service SHALL возвращать HTTP статус 200 с полями: status="success", result.photo_url, result.processing_time, result.result_id
5. THE GPT Service SHALL предоставить эндпоинт GET /v1/photo/history/{telegram_id} для получения истории обработок
6. WHEN запрос отправляется на GET /v1/photo/history/{telegram_id}, THE GPT Service SHALL принимать query параметры: limit, offset
7. IF API ключ неверный, THEN THE GPT Service SHALL возвращать HTTP статус 403 с сообщением об ошибке

### Requirement 11: Конфигурация и переменные окружения

**User Story:** Как администратор системы, я хочу настраивать параметры сервиса через переменные окружения, чтобы легко управлять конфигурацией.

#### Acceptance Criteria

1. THE Photo Processing System SHALL использовать переменную окружения IMAGE_GEN_API_KEY для аутентификации в Image Generation API
2. THE Photo Processing System SHALL использовать переменную окружения IMAGE_GEN_BASE_URL для определения базового URL Image Generation API
3. THE Photo Processing System SHALL использовать переменную окружения IMAGE_GEN_TIMEOUT для установки таймаута запросов (по умолчанию 60 секунд)
4. THE Photo Processing System SHALL использовать переменную окружения IMAGE_GEN_MAX_RETRIES для установки максимального количества повторных попыток (по умолчанию 3)
5. THE Photo Processing System SHALL использовать переменную окружения PHOTO_STORAGE_TYPE со значением "url" для определения типа хранения результатов
