# Этап 3: Модуль векторного поиска

## 📋 Обзор этапа

**Цель:** Реализовать поиск релевантных чанков по запросу пользователя с использованием векторного поиска в pgvector.

**Длительность:** 2-3 дня

**Зависимости:** Этап 1 (инфраструктура), Этап 2 (индексация должна быть выполнена хотя бы для одного кабинета)

**Результат:** Модуль, который может находить наиболее релевантные чанки данных для запроса пользователя.

---

## 🎯 Задачи этапа

### Задача 3.1: Создание структуры модуля поиска

#### Описание
Создание основного класса `VectorSearch` с методами для векторного поиска.

#### Файл: `gpt_integration/ai_chat/rag/vector_search.py`

**Структура класса:**

```python
"""
Vector Search - модуль для векторного поиска в pgvector.
"""

import logging
from typing import List, Dict, Any, Optional
from sqlalchemy.orm import Session
from sqlalchemy import text
from openai import OpenAI

from .database import get_rag_db, RAGSessionLocal
from .models import RAGEmbedding, RAGMetadata

logger = logging.getLogger(__name__)


class VectorSearch:
    """
    Класс для векторного поиска релевантных чанков.
    
    Процесс поиска:
    1. Генерация эмбеддинга для запроса пользователя
    2. Векторный поиск в pgvector
    3. Получение метаданных для найденных векторов
    """
    
    def __init__(
        self,
        openai_client: Optional[OpenAI] = None,
        embeddings_model: str = "text-embedding-3-small",
        similarity_threshold: float = 0.7
    ):
        """
        Инициализация поиска.
        
        Args:
            openai_client: Клиент OpenAI
            embeddings_model: Модель для генерации эмбеддингов
            similarity_threshold: Минимальный порог релевантности (0-1)
        """
        self.openai_client = openai_client or OpenAI()
        self.embeddings_model = embeddings_model
        self.similarity_threshold = similarity_threshold
    
    def generate_query_embedding(self, query_text: str) -> List[float]:
        """
        Генерация эмбеддинга для запроса пользователя.
        
        Args:
            query_text: Текст запроса пользователя
            
        Returns:
            Вектор размерности 1536
        """
        # Реализация в задаче 3.2
        pass
    
    def search(
        self,
        query_embedding: List[float],
        cabinet_id: int,
        chunk_types: Optional[List[str]] = None,
        limit: int = 5
    ) -> List[Dict[str, Any]]:
        """
        Векторный поиск в pgvector.
        
        Args:
            query_embedding: Эмбеддинг запроса
            cabinet_id: ID кабинета (обязательная фильтрация)
            chunk_types: Список типов чанков для фильтрации (опционально)
            limit: Максимальное количество результатов
            
        Returns:
            Список словарей с результатами:
            [
                {
                    'embedding_id': 1,
                    'metadata_id': 1,
                    'similarity': 0.85,
                    'distance': 0.15
                },
                ...
            ]
        """
        # Реализация в задаче 3.3
        pass
    
    def get_metadata_for_embeddings(
        self,
        embedding_ids: List[int],
        db: Session
    ) -> List[Dict[str, Any]]:
        """
        Получение метаданных для найденных embedding IDs.
        
        Args:
            embedding_ids: Список ID эмбеддингов
            db: Сессия БД
            
        Returns:
            Список словарей с метаданными:
            [
                {
                    'id': 1,
                    'chunk_text': '...',
                    'chunk_type': 'order',
                    'source_table': 'wb_orders',
                    'source_id': 123,
                    'similarity': 0.85
                },
                ...
            ]
        """
        # Реализация в задаче 3.4
        pass
    
    def search_relevant_chunks(
        self,
        query_text: str,
        cabinet_id: int,
        chunk_types: Optional[List[str]] = None,
        max_chunks: int = 5
    ) -> List[Dict[str, Any]]:
        """
        Главный метод поиска релевантных чанков.
        
        Объединяет все этапы поиска.
        
        Args:
            query_text: Текст запроса пользователя
            cabinet_id: ID кабинета
            chunk_types: Типы чанков для фильтрации (опционально)
            max_chunks: Максимальное количество результатов
            
        Returns:
            Список релевантных чанков с метаданными
        """
        # Реализация в задаче 3.5
        pass
```

#### Действия

1. **Создать файл `gpt_integration/ai_chat/rag/vector_search.py`**
   - Создать класс `VectorSearch`
   - Определить все методы (пока с `pass`)
   - Добавить docstrings и type hints

2. **Добавить импорты**
   - SQLAlchemy для работы с БД
   - OpenAI для генерации эмбеддингов
   - Логирование

#### Критерии готовности
- ✅ Файл `vector_search.py` создан
- ✅ Класс `VectorSearch` определен
- ✅ Все методы определены (пока без реализации)
- ✅ Импорты добавлены

---

### Задача 3.2: Генерация эмбеддинга запроса

#### Описание
Реализация метода для генерации векторного представления текста запроса пользователя. Использует ту же модель, что и при индексации.

#### Реализация

```python
def generate_query_embedding(self, query_text: str) -> List[float]:
    """
    Генерация эмбеддинга для запроса пользователя.
    
    Использует ту же модель, что и при индексации, для обеспечения
    совместимости векторов.
    
    Args:
        query_text: Текст запроса пользователя
        
    Returns:
        Вектор размерности 1536
    """
    if not query_text or not query_text.strip():
        raise ValueError("Запрос не может быть пустым")
    
    try:
        logger.info(f"Генерация эмбеддинга для запроса: '{query_text[:50]}...'")
        
        # Вызов OpenAI Embeddings API
        response = self.openai_client.embeddings.create(
            model=self.embeddings_model,
            input=[query_text]  # Один запрос
        )
        
        # Извлечь вектор из ответа
        embedding = response.data[0].embedding
        
        logger.info(f"✅ Эмбеддинг сгенерирован: размерность {len(embedding)}")
        
        return embedding
        
    except Exception as e:
        logger.error(f"❌ Ошибка при генерации эмбеддинга: {e}")
        raise
```

#### Особенности

1. **Один запрос:** Генерируется эмбеддинг только для одного текста
2. **Та же модель:** Используется та же модель, что и при индексации
3. **Обработка ошибок:** Логирование и проброс исключений

#### Действия

1. **Реализовать метод `generate_query_embedding()`**
   - Вызвать OpenAI API
   - Извлечь вектор
   - Обработать ошибки

2. **Протестировать**
   - Создать тестовый запрос
   - Вызвать метод
   - Проверить размерность вектора (1536)

#### Критерии готовности
- ✅ Метод реализован
- ✅ Эмбеддинг генерируется корректно
- ✅ Размерность = 1536
- ✅ Ошибки обрабатываются

---

### Задача 3.3: Векторный поиск в pgvector

#### Описание
Реализация SQL запроса для векторного поиска с использованием операторов pgvector.

#### Операторы pgvector

- `<->` — L2 distance (евклидово расстояние)
- `<#>` — cosine distance (косинусное расстояние, рекомендуется для эмбеддингов)
- `<=>` — inner product

Для эмбеддингов OpenAI рекомендуется использовать **cosine distance** (`<#>`).

**Формула similarity:**
```
similarity = 1 - cosine_distance
```

#### SQL запрос

```sql
SELECT 
    e.id AS embedding_id,
    e.metadata_id,
    1 - (e.embedding <#> :query_embedding::vector) AS similarity,
    e.embedding <#> :query_embedding::vector AS distance
FROM rag_embeddings e
JOIN rag_metadata m ON e.metadata_id = m.id
WHERE m.cabinet_id = :cabinet_id
  AND (:chunk_types IS NULL OR m.chunk_type = ANY(:chunk_types))
ORDER BY e.embedding <#> :query_embedding::vector
LIMIT :limit;
```

#### Реализация

```python
def search(
    self,
    query_embedding: List[float],
    cabinet_id: int,
    chunk_types: Optional[List[str]] = None,
    limit: int = 5
) -> List[Dict[str, Any]]:
    """
    Векторный поиск в pgvector.
    
    Использует cosine distance для поиска наиболее похожих векторов.
    """
    db = RAGSessionLocal()
    
    try:
        # Преобразовать список в строку для PostgreSQL
        embedding_str = '[' + ','.join(map(str, query_embedding)) + ']'
        
        # Построить SQL запрос
        query = text("""
            SELECT 
                e.id AS embedding_id,
                e.metadata_id,
                1 - (e.embedding <#> :query_embedding::vector) AS similarity,
                e.embedding <#> :query_embedding::vector AS distance
            FROM rag_embeddings e
            JOIN rag_metadata m ON e.metadata_id = m.id
            WHERE m.cabinet_id = :cabinet_id
              AND (:chunk_types::text[] IS NULL OR m.chunk_type = ANY(:chunk_types::text[]))
            ORDER BY e.embedding <#> :query_embedding::vector
            LIMIT :limit
        """)
        
        # Параметры запроса
        params = {
            'query_embedding': embedding_str,
            'cabinet_id': cabinet_id,
            'chunk_types': chunk_types if chunk_types else None,
            'limit': limit
        }
        
        # Выполнить запрос
        result = db.execute(query, params)
        rows = result.fetchall()
        
        # Преобразовать результаты
        results = []
        for row in rows:
            similarity = float(row.similarity)
            
            # Применить порог релевантности
            if similarity >= self.similarity_threshold:
                results.append({
                    'embedding_id': row.embedding_id,
                    'metadata_id': row.metadata_id,
                    'similarity': similarity,
                    'distance': float(row.distance)
                })
        
        logger.info(
            f"Найдено {len(results)} релевантных результатов "
            f"(порог: {self.similarity_threshold})"
        )
        
        return results
        
    except Exception as e:
        logger.error(f"❌ Ошибка при векторном поиске: {e}")
        raise
        
    finally:
        db.close()
```

#### Альтернативный вариант через SQLAlchemy ORM

Если предпочтительнее использовать ORM:

```python
from sqlalchemy import func
from pgvector.sqlalchemy import Vector

# Использовать raw SQL все равно проще для векторного поиска
# Но можно попробовать через ORM:

results = db.query(
    RAGEmbedding.id.label('embedding_id'),
    RAGEmbedding.metadata_id,
    func.cast(
        1 - (RAGEmbedding.embedding.op('<#>')(':query_embedding')),
        Float
    ).label('similarity')
).join(
    RAGMetadata,
    RAGEmbedding.metadata_id == RAGMetadata.id
).filter(
    RAGMetadata.cabinet_id == cabinet_id
).order_by(
    RAGEmbedding.embedding.op('<#>')(':query_embedding')
).limit(limit).all()
```

**Рекомендация:** Использовать raw SQL для векторного поиска, так как это проще и понятнее.

#### Оптимизация: Использование HNSW индекса

HNSW индекс автоматически используется PostgreSQL при выполнении запроса с оператором `<#>` или `<->`. Убедиться, что индекс создан:

```sql
CREATE INDEX idx_rag_embeddings_vector 
ON rag_embeddings 
USING hnsw (embedding vector_cosine_ops)
WITH (m = 16, ef_construction = 64);
```

#### Действия

1. **Реализовать метод `search()`**
   - Создать SQL запрос с векторным поиском
   - Применить фильтрацию по `cabinet_id` и `chunk_types`
   - Применить порог релевантности
   - Вернуть отсортированные результаты

2. **Протестировать**
   - Создать тестовый эмбеддинг
   - Вызвать метод
   - Проверить результаты
   - Проверить производительность (< 100ms)

#### Критерии готовности
- ✅ Метод реализован
- ✅ Векторный поиск работает
- ✅ Фильтрация по `cabinet_id` работает
- ✅ Фильтрация по `chunk_types` работает (если передана)
- ✅ Порог релевантности применяется
- ✅ Результаты отсортированы по similarity
- ✅ Производительность приемлемая

---

### Задача 3.4: Получение метаданных для результатов

#### Описание
Реализация метода для получения полных метаданных (включая текст чанка) для найденных embedding IDs.

#### Реализация

```python
def get_metadata_for_embeddings(
    self,
    embedding_ids: List[int],
    db: Session
) -> List[Dict[str, Any]]:
    """
    Получение метаданных для найденных embedding IDs.
    
    Args:
        embedding_ids: Список ID эмбеддингов
        db: Сессия БД
        
    Returns:
        Список словарей с метаданными
    """
    if not embedding_ids:
        return []
    
    try:
        # Запрос метаданных через JOIN
        results = db.query(
            RAGMetadata.id,
            RAGMetadata.chunk_text,
            RAGMetadata.chunk_type,
            RAGMetadata.source_table,
            RAGMetadata.source_id,
            RAGEmbedding.id.label('embedding_id')
        ).join(
            RAGEmbedding,
            RAGMetadata.id == RAGEmbedding.metadata_id
        ).filter(
            RAGEmbedding.id.in_(embedding_ids)
        ).all()
        
        # Преобразовать в список словарей
        metadata_list = []
        for row in results:
            metadata_list.append({
                'id': row.id,
                'embedding_id': row.embedding_id,
                'chunk_text': row.chunk_text,
                'chunk_type': row.chunk_type,
                'source_table': row.source_table,
                'source_id': row.source_id
            })
        
        logger.info(f"Получено {len(metadata_list)} метаданных")
        
        return metadata_list
        
    except Exception as e:
        logger.error(f"❌ Ошибка при получении метаданных: {e}")
        raise
```

#### Объединение с результатами поиска

Можно объединить similarity из результатов поиска с метаданными:

```python
def get_metadata_with_similarity(
    self,
    search_results: List[Dict[str, Any]],
    db: Session
) -> List[Dict[str, Any]]:
    """
    Получение метаданных с similarity из результатов поиска.
    """
    embedding_ids = [r['embedding_id'] for r in search_results]
    metadata_list = self.get_metadata_for_embeddings(embedding_ids, db)
    
    # Создать словарь для быстрого поиска
    similarity_map = {r['embedding_id']: r['similarity'] for r in search_results}
    
    # Добавить similarity к метаданным
    for metadata in metadata_list:
        embedding_id = metadata['embedding_id']
        metadata['similarity'] = similarity_map.get(embedding_id, 0.0)
    
    # Сортировать по similarity (от большего к меньшему)
    metadata_list.sort(key=lambda x: x['similarity'], reverse=True)
    
    return metadata_list
```

#### Действия

1. **Реализовать метод `get_metadata_for_embeddings()`**
   - Выполнить JOIN запрос
   - Вернуть метаданные

2. **Создать метод `get_metadata_with_similarity()`** (опционально)
   - Объединить результаты поиска с метаданными
   - Добавить similarity

3. **Протестировать**
   - Создать тестовые embedding IDs
   - Вызвать метод
   - Проверить полученные метаданные

#### Критерии готовности
- ✅ Метод реализован
- ✅ Метаданные получаются корректно
- ✅ Связь с embedding работает
- ✅ Данные структурированы правильно

---

### Задача 3.5: Главный метод поиска

#### Описание
Реализация метода `search_relevant_chunks()`, который объединяет все этапы поиска.

#### Реализация

```python
def search_relevant_chunks(
    self,
    query_text: str,
    cabinet_id: int,
    chunk_types: Optional[List[str]] = None,
    max_chunks: int = 5
) -> List[Dict[str, Any]]:
    """
    Главный метод поиска релевантных чанков.
    
    Объединяет все этапы:
    1. Генерация эмбеддинга запроса
    2. Векторный поиск
    3. Получение метаданных
    
    Args:
        query_text: Текст запроса пользователя
        cabinet_id: ID кабинета
        chunk_types: Типы чанков для фильтрации (опционально)
        max_chunks: Максимальное количество результатов
        
    Returns:
        Список релевантных чанков с метаданными и similarity
    """
    try:
        logger.info(
            f"Поиск релевантных чанков: "
            f"cabinet_id={cabinet_id}, "
            f"query='{query_text[:50]}...', "
            f"chunk_types={chunk_types}, "
            f"max_chunks={max_chunks}"
        )
        
        # 1. Генерация эмбеддинга запроса
        query_embedding = self.generate_query_embedding(query_text)
        
        # 2. Векторный поиск
        search_results = self.search(
            query_embedding=query_embedding,
            cabinet_id=cabinet_id,
            chunk_types=chunk_types,
            limit=max_chunks
        )
        
        if not search_results:
            logger.info("Релевантные чанки не найдены")
            return []
        
        # 3. Получение метаданных
        db = RAGSessionLocal()
        try:
            embedding_ids = [r['embedding_id'] for r in search_results]
            metadata_list = self.get_metadata_for_embeddings(embedding_ids, db)
            
            # Объединить с similarity
            similarity_map = {r['embedding_id']: r['similarity'] for r in search_results}
            for metadata in metadata_list:
                embedding_id = metadata['embedding_id']
                metadata['similarity'] = similarity_map.get(embedding_id, 0.0)
            
            # Сортировать по similarity
            metadata_list.sort(key=lambda x: x['similarity'], reverse=True)
            
            logger.info(
                f"✅ Найдено {len(metadata_list)} релевантных чанков "
                f"(similarity: {metadata_list[0]['similarity']:.2f} - "
                f"{metadata_list[-1]['similarity']:.2f})"
            )
            
            return metadata_list
            
        finally:
            db.close()
            
    except Exception as e:
        logger.error(f"❌ Ошибка при поиске релевантных чанков: {e}")
        # Вернуть пустой список при ошибке (fallback)
        return []
```

#### Обработка ошибок

Метод должен обрабатывать ошибки gracefully:
- При ошибке генерации эмбеддинга → вернуть пустой список
- При ошибке поиска → вернуть пустой список
- При ошибке получения метаданных → вернуть пустой список

Это позволяет системе работать даже при сбоях RAG (fallback на обычный промпт).

#### Действия

1. **Реализовать метод `search_relevant_chunks()`**
   - Объединить все этапы
   - Обработать ошибки

2. **Протестировать end-to-end**
   - Вызвать метод с тестовым запросом
   - Проверить результаты
   - Проверить обработку ошибок

#### Критерии готовности
- ✅ Метод реализован
- ✅ Все этапы выполняются последовательно
- ✅ Результаты корректны
- ✅ Ошибки обрабатываются
- ✅ End-to-end тест проходит

---

## ✅ Критерии готовности Этапа 3

### Общие критерии

- ✅ Модуль векторного поиска создан
- ✅ Генерация эмбеддинга запроса работает
- ✅ Векторный поиск в pgvector работает
- ✅ Фильтрация по `cabinet_id` и `chunk_types` работает
- ✅ Порог релевантности применяется
- ✅ Метаданные получаются корректно
- ✅ End-to-end поиск работает

### Тестирование

**1. Тест генерации эмбеддинга:**
```python
search = VectorSearch()
embedding = search.generate_query_embedding("Какие товары продаются лучше всего?")
assert len(embedding) == 1536
```

**2. Тест векторного поиска:**
```python
results = search.search(
    query_embedding=embedding,
    cabinet_id=1,
    limit=5
)
assert len(results) > 0
assert all(r['similarity'] >= 0.7 for r in results)  # Порог релевантности
```

**3. Тест полного поиска:**
```python
chunks = search.search_relevant_chunks(
    query_text="Какие товары продаются лучше всего?",
    cabinet_id=1,
    max_chunks=5
)
assert len(chunks) > 0
assert all('chunk_text' in chunk for chunk in chunks)
assert all('similarity' in chunk for chunk in chunks)
```

**4. Тест производительности:**
```python
import time
start = time.time()
chunks = search.search_relevant_chunks(...)
elapsed = time.time() - start
assert elapsed < 0.1  # Меньше 100ms
```

---

## 🐛 Возможные проблемы и решения

### Проблема 1: Медленный поиск

**Симптомы:** Поиск занимает > 100ms

**Решения:**
- Проверить наличие HNSW индекса
- Уменьшить `limit` (количество результатов)
- Проверить размер таблицы (возможно, нужна оптимизация)

### Проблема 2: Низкая релевантность результатов

**Симптомы:** Найденные чанки не релевантны запросу

**Решения:**
- Проверить, что используется та же модель эмбеддингов, что и при индексации
- Уменьшить порог релевантности (если слишком высокий)
- Проверить качество индексации (правильно ли созданы чанки)

### Проблема 3: Ошибка при выполнении SQL запроса

**Симптомы:** Ошибка при векторном поиске

**Решения:**
- Проверить формат embedding (должен быть массив float)
- Проверить размерность embedding (должна быть 1536)
- Проверить наличие расширения pgvector

---

**Версия:** 1.0.0  
**Дата:** 2025-01-XX  
**Статус:** Детальный план Этапа 3

