# Этап 8: Тестирование и оптимизация

## 📋 Обзор этапа

**Цель:** Протестировать систему и оптимизировать производительность.

**Длительность:** 2-3 дня

**Зависимости:** Все предыдущие этапы

**Результат:** Протестированная и оптимизированная RAG система.

---

## 🎯 Задачи этапа

### Задача 8.1: Unit тесты

**Файлы:**
- `tests/test_rag_indexer.py`
- `tests/test_vector_search.py`
- `tests/test_context_builder.py`
- `tests/test_query_processor.py` (если реализован)

**Примеры тестов:**

**Тест индексации:**
```python
def test_index_cabinet():
    indexer = RAGIndexer()
    result = await indexer.index_cabinet(cabinet_id=1)
    assert result['success'] == True
    assert result['total_chunks'] > 0
```

**Тест векторного поиска:**
```python
def test_vector_search():
    search = VectorSearch()
    chunks = search.search_relevant_chunks(
        query_text="Какие товары продаются лучше всего?",
        cabinet_id=1
    )
    assert len(chunks) > 0
    assert all('chunk_text' in chunk for chunk in chunks)
```

---

### Задача 8.2: Integration тесты

**Тесты:**
- Полная индексация кабинета
- Векторный поиск после индексации
- Формирование контекста
- Интеграция с AI Chat Service

---

### Задача 8.3: Производительность

**Метрики:**
- Время векторного поиска: < 100ms
- Время индексации кабинета: < 5 минут для 10K чанков
- Использование памяти

**Оптимизации:**
- Проверить индексы в БД
- Оптимизировать SQL запросы
- Настроить параметры HNSW индекса

---

### Задача 8.4: Оптимизация параметров

**Параметры для настройки:**
- `RAG_SIMILARITY_THRESHOLD` (тестирование различных значений)
- `RAG_MAX_CHUNKS` (баланс между релевантностью и размером)
- `RAG_CONTEXT_MAX_LENGTH`

**Метод:**
- A/B тестирование различных значений
- Измерение качества ответов
- Выбор оптимальных параметров

---

## ✅ Критерии готовности

- ✅ Unit тесты написаны и проходят
- ✅ Integration тесты написаны и проходят
- ✅ Производительность соответствует требованиям
- ✅ Параметры оптимизированы

---

**Версия:** 1.0.0  
**Дата:** 2025-01-XX  
**Статус:** Детальный план Этапа 8

