# RAG-ассистент - Поэтапная разработка

## 📋 Обзор

Этот документ содержит детальный план разработки RAG-модуля для AI Chat Service. План разбит на этапы с конкретными задачами, чек-листами и критериями готовности.

**Связь с основным ТЗ:** См. `docs/RAG/RAG.md` для полного технического задания.

---

## 🎯 Этап 1: Подготовка инфраструктуры

**Цель:** Настроить векторную базу данных и создать необходимые таблицы.

**Длительность:** 1-2 дня

### Задачи

#### 1.1. Установка расширения pgvector в PostgreSQL

**Действия:**
- [ ] Проверить версию PostgreSQL (требуется >= 11)
- [ ] Установить расширение pgvector на сервере БД
- [ ] Создать расширение в базе данных: `CREATE EXTENSION IF NOT EXISTS vector;`
- [ ] Проверить установку: `SELECT * FROM pg_extension WHERE extname = 'vector';`

**Проверка:**
```sql
-- Должно вернуть версию расширения
SELECT extversion FROM pg_extension WHERE extname = 'vector';
```

**Документация:**
- Сохранить версию pgvector в документации
- Записать команды установки для других окружений

---

#### 1.2. Создание таблиц для RAG

**Файл:** `gpt_integration/ai_chat/rag/models.py`

**Задачи:**
- [ ] Создать SQLAlchemy модель `RAGEmbedding` для таблицы `rag_embeddings`
  - Поля: `id`, `embedding` (vector(1536)), `metadata_id` (FK), `created_at`, `updated_at`
- [ ] Создать SQLAlchemy модель `RAGMetadata` для таблицы `rag_metadata`
  - Поля: `id`, `cabinet_id`, `source_table`, `source_id`, `chunk_type`, `chunk_text`, `created_at`, `updated_at`
  - Уникальное ограничение: `(cabinet_id, source_table, source_id)`
- [ ] Создать SQLAlchemy модель `RAGIndexStatus` для таблицы `rag_index_status`
  - Поля: `id`, `cabinet_id` (UNIQUE), `last_indexed_at`, `last_incremental_at`, `indexing_status`, `total_chunks`, `created_at`, `updated_at`

**Структура файла:**
```python
# models.py структура (без реализации)
# - Импорты: Base, Column, Integer, String, Text, DateTime, ForeignKey, Index, UniqueConstraint
# - RAGEmbedding класс
# - RAGMetadata класс  
# - RAGIndexStatus класс
```

**Проверка:**
- [ ] Модели корректно определяют все поля
- [ ] Тип `vector(1536)` правильно указан для embedding
- [ ] Индексы и ограничения определены

---

#### 1.3. Создание миграций базы данных

**Задачи:**
- [ ] Создать SQL скрипт миграции для создания таблиц
- [ ] Или использовать Alembic (если уже используется в проекте)
- [ ] Создать индексы:
  - HNSW индекс на `rag_embeddings.embedding`
  - Индексы на `rag_metadata.cabinet_id`, `rag_metadata.chunk_type`
  - Индекс на `rag_index_status.cabinet_id`

**SQL скрипт (пример структуры):**
```sql
-- Создание таблиц
CREATE TABLE rag_metadata (...);
CREATE TABLE rag_embeddings (...);
CREATE TABLE rag_index_status (...);

-- Создание индексов
CREATE INDEX idx_rag_metadata_cabinet_type ON rag_metadata(cabinet_id, chunk_type);
CREATE INDEX idx_rag_embeddings_metadata ON rag_embeddings(metadata_id);
-- HNSW индекс на embedding (пример)
CREATE INDEX idx_rag_embeddings_vector ON rag_embeddings 
USING hnsw (embedding vector_cosine_ops);
```

**Проверка:**
- [ ] Миграция успешно выполняется на тестовой БД
- [ ] Все индексы созданы
- [ ] Можно вставить тестовую запись в каждую таблицу

---

#### 1.4. Настройка подключения к векторной БД

**Файл:** `gpt_integration/ai_chat/rag/database.py`

**Задачи:**
- [ ] Создать функцию для получения URL векторной БД из переменных окружения
  - Приоритет 1: `RAG_VECTOR_DB_URL`
  - Приоритет 2: `DATABASE_URL` (основная БД)
- [ ] Создать отдельный engine для векторной БД (или использовать существующий)
- [ ] Создать SessionLocal для векторной БД
- [ ] Создать функцию `get_rag_db()` для dependency injection

**Проверка:**
- [ ] Подключение к БД работает
- [ ] Можно выполнить запрос к таблицам RAG
- [ ] Переменные окружения правильно читаются

---

#### 1.5. Обновление переменных окружения

**Файлы:** `.env`, `docker-compose.yml`, `env_example.txt`

**Задачи:**
- [ ] Добавить переменную `RAG_VECTOR_DB_URL` (опционально, может совпадать с DATABASE_URL)
- [ ] Добавить переменную `RAG_ENABLED=true`
- [ ] Добавить переменную `OPENAI_EMBEDDINGS_MODEL=text-embedding-3-small`
- [ ] Добавить опциональные переменные:
  - `RAG_MAX_CHUNKS=5`
  - `RAG_SIMILARITY_THRESHOLD=0.7`
  - `RAG_CONTEXT_MAX_LENGTH=3000`
  - `RAG_INDEXING_INTERVAL_HOURS=6`
  - `RAG_EMBEDDING_BATCH_SIZE=100`

**Проверка:**
- [ ] Все переменные добавлены в `.env.example`
- [ ] Docker-compose обновлен
- [ ] Документация обновлена

---

### Критерии готовности Этапа 1

- ✅ Расширение pgvector установлено и работает
- ✅ Все три таблицы созданы в БД
- ✅ Индексы созданы (включая HNSW на embedding)
- ✅ Модели SQLAlchemy созданы и протестированы
- ✅ Подключение к векторной БД работает
- ✅ Переменные окружения настроены

**Тестирование:**
- Вставить тестовую запись в каждую таблицу
- Выполнить простой векторный поиск (даже с тестовыми данными)

---

## 🎯 Этап 2: Модуль индексации данных

**Цель:** Реализовать сервис для индексации данных из основной БД в векторную БД.

**Длительность:** 3-4 дня

### Задачи

#### 2.1. Создание структуры модуля индексации

**Файл:** `gpt_integration/ai_chat/rag/indexer.py`

**Структура модуля:**
- [ ] Создать класс `RAGIndexer`
- [ ] Метод `__init__()` — инициализация с подключениями к БД
- [ ] Метод `extract_data_from_main_db(cabinet_id)` — извлечение данных
- [ ] Метод `create_chunks(data)` — создание текстовых чанков
- [ ] Метод `generate_embeddings(chunks)` — генерация эмбеддингов через OpenAI
- [ ] Метод `save_to_vector_db(embeddings, metadata)` — сохранение в векторную БД
- [ ] Метод `index_cabinet(cabinet_id)` — главный метод индексации кабинета

---

#### 2.2. Извлечение данных из основной БД

**Метод:** `extract_data_from_main_db(cabinet_id)`

**Задачи:**
- [ ] Реализовать извлечение данных из `wb_orders`
  - Фильтр: `cabinet_id = ?` и `order_date >= NOW() - INTERVAL '90 days'`
  - Поля: `id`, `order_id`, `nm_id`, `name`, `size`, `price`, `total_price`, `order_date`, `status`
- [ ] Реализовать извлечение данных из `wb_products`
  - Фильтр: `cabinet_id = ?`
  - Поля: `nm_id`, `name`, `brand`, `category`, `price`, `rating`, `reviews_count`
- [ ] Реализовать извлечение данных из `wb_stocks`
  - Фильтр: `cabinet_id = ?`
  - Поля: `nm_id`, `size`, `warehouse_name`, `quantity`
- [ ] Реализовать извлечение данных из `wb_reviews`
  - Фильтр: `cabinet_id = ?` и `created_at >= NOW() - INTERVAL '90 days'`
  - Поля: `id`, `nm_id`, `rating`, `text`, `created_at`
- [ ] Реализовать извлечение данных из `wb_sales`
  - Фильтр: `cabinet_id = ?` и `sale_date >= NOW() - INTERVAL '90 days'`
  - Поля: `id`, `nm_id`, `type`, `sale_date`, `price`

**Проверка:**
- [ ] Все запросы возвращают данные для тестового кабинета
- [ ] Нет SQL injection уязвимостей (использование параметризованных запросов)
- [ ] Обработка случаев, когда данных нет

---

#### 2.3. Создание текстовых чанков

**Метод:** `create_chunks(data)`

**Задачи:**
- [ ] Реализовать функцию `create_order_chunk(order)` — преобразование заказа в текст
  - Формат: "Заказ #{order_id} от {date}: товар '{name}' (nm_id: {nm_id}), размер {size}, цена {price}₽, статус: {status}"
- [ ] Реализовать функцию `create_product_chunk(product)` — преобразование товара в текст
  - Формат: "Товар '{name}' (nm_id: {nm_id}), бренд: {brand}, категория: {category}, рейтинг: {rating}, отзывов: {reviews_count}, цена: {price}₽"
- [ ] Реализовать функцию `create_stock_chunk(stock)` — преобразование остатка в текст
  - Формат: "Остаток товара '{name}' (nm_id: {nm_id}), размер {size}, склад: {warehouse}, количество: {quantity} шт"
- [ ] Реализовать функцию `create_review_chunk(review)` — преобразование отзыва в текст
  - Формат: "Отзыв на товар '{name}' (nm_id: {nm_id}): рейтинг {rating}⭐, дата: {date}, текст: '{text}'"
- [ ] Реализовать функцию `create_sale_chunk(sale)` — преобразование продажи в текст
  - Формат: "Продажа товара '{name}' (nm_id: {nm_id}) от {date}: тип - {type}, сумма: {price}₽"

**Проверка:**
- [ ] Все функции создают читаемые текстовые чанки
- [ ] Обработка NULL значений в данных
- [ ] Длина чанков разумная (не слишком длинные)

---

#### 2.4. Генерация эмбеддингов

**Метод:** `generate_embeddings(chunks)`

**Задачи:**
- [ ] Создать функцию для вызова OpenAI Embeddings API
- [ ] Реализовать batch processing (группировка чанков для экономии запросов)
  - Размер батча из переменной окружения `RAG_EMBEDDING_BATCH_SIZE` (default: 100)
- [ ] Обработка ошибок API (retry логика, rate limits)
- [ ] Логирование прогресса генерации эмбеддингов

**Структура:**
```python
# Псевдокод структуры
def generate_embeddings(chunks: List[str]) -> List[List[float]]:
    # 1. Разбить chunks на батчи
    # 2. Для каждого батча:
    #    - Вызвать OpenAI Embeddings API
    #    - Получить векторы
    # 3. Вернуть список векторов
```

**Проверка:**
- [ ] Эмбеддинги генерируются для всех чанков
- [ ] Размерность векторов = 1536
- [ ] Batch processing работает корректно
- [ ] Обработка ошибок работает

---

#### 2.5. Сохранение в векторную БД

**Метод:** `save_to_vector_db(embeddings, metadata)`

**Задачи:**
- [ ] Реализовать сохранение метаданных в `rag_metadata`
  - Проверка на дубликаты (UNIQUE constraint)
  - Обновление существующих записей при необходимости
- [ ] Реализовать сохранение эмбеддингов в `rag_embeddings`
  - Связь с метаданными через `metadata_id`
- [ ] Использовать транзакции для атомарности операций
- [ ] Обработка ошибок (rollback при ошибках)

**Проверка:**
- [ ] Данные сохраняются корректно
- [ ] Нет дубликатов в `rag_metadata`
- [ ] Связь между `rag_embeddings` и `rag_metadata` работает
- [ ] Транзакции работают корректно

---

#### 2.6. Главный метод индексации

**Метод:** `index_cabinet(cabinet_id)`

**Задачи:**
- [ ] Проверить статус индексации в `rag_index_status`
  - Если статус 'in_progress' — пропустить или подождать
- [ ] Установить статус 'in_progress'
- [ ] Вызвать все методы по порядку:
  1. `extract_data_from_main_db(cabinet_id)`
  2. `create_chunks(data)`
  3. `generate_embeddings(chunks)`
  4. `save_to_vector_db(embeddings, metadata)`
- [ ] Обновить статус 'completed' с датой и количеством чанков
- [ ] Обработка ошибок: установить статус 'failed', логировать

**Проверка:**
- [ ] Полная индексация кабинета работает end-to-end
- [ ] Статус обновляется корректно
- [ ] Ошибки обрабатываются и логируются

---

#### 2.7. Инкрементальная индексация

**Метод:** `incremental_index_cabinet(cabinet_id)`

**Задачи:**
- [ ] Получить `last_incremental_at` из `rag_index_status`
- [ ] Извлечь только новые/измененные записи (WHERE `updated_at > last_incremental_at`)
- [ ] Удалить старые чанки для обновленных записей (если нужно)
- [ ] Создать новые чанки и эмбеддинги
- [ ] Обновить `last_incremental_at`

**Проверка:**
- [ ] Инкрементальная индексация работает
- [ ] Обновляются только измененные данные
- [ ] Старые данные не дублируются

---

### Критерии готовности Этапа 2

- ✅ Модуль индексации создан и структурирован
- ✅ Извлечение данных из всех таблиц работает
- ✅ Создание текстовых чанков работает для всех типов данных
- ✅ Генерация эмбеддингов через OpenAI API работает
- ✅ Сохранение в векторную БД работает
- ✅ Полная индексация кабинета работает end-to-end
- ✅ Инкрементальная индексация работает

**Тестирование:**
- Проиндексировать тестовый кабинет с реальными данными
- Проверить количество созданных чанков
- Проверить наличие эмбеддингов в БД

---

## 🎯 Этап 3: Модуль векторного поиска

**Цель:** Реализовать поиск релевантных чанков по запросу пользователя.

**Длительность:** 2-3 дня

### Задачи

#### 3.1. Создание структуры модуля поиска

**Файл:** `gpt_integration/ai_chat/rag/vector_search.py`

**Структура модуля:**
- [ ] Создать класс `VectorSearch`
- [ ] Метод `__init__()` — инициализация с подключением к БД
- [ ] Метод `generate_query_embedding(query_text)` — генерация эмбеддинга для запроса
- [ ] Метод `search(query_embedding, cabinet_id, chunk_types=None, limit=5)` — векторный поиск
- [ ] Метод `get_metadata_for_embeddings(embedding_ids)` — получение метаданных

---

#### 3.2. Генерация эмбеддинга запроса

**Метод:** `generate_query_embedding(query_text)`

**Задачи:**
- [ ] Вызвать OpenAI Embeddings API для текста запроса
- [ ] Использовать ту же модель, что и при индексации (`text-embedding-3-small`)
- [ ] Вернуть вектор размерности 1536
- [ ] Обработка ошибок API

**Проверка:**
- [ ] Эмбеддинг генерируется корректно
- [ ] Размерность = 1536
- [ ] Ошибки обрабатываются

---

#### 3.3. Векторный поиск в pgvector

**Метод:** `search(query_embedding, cabinet_id, chunk_types=None, limit=5)`

**Задачи:**
- [ ] Реализовать SQL запрос с использованием оператора `<#>` (cosine distance) или `<->` (L2)
- [ ] Фильтрация по `cabinet_id` (обязательно)
- [ ] Опциональная фильтрация по `chunk_types` (если передан список)
- [ ] Сортировка по similarity (от большего к меньшему)
- [ ] Ограничение количества результатов (LIMIT)
- [ ] Применение порога релевантности (`RAG_SIMILARITY_THRESHOLD`)

**SQL структура (концептуально):**
```sql
SELECT 
    e.id,
    e.embedding <#> %s::vector AS distance,
    1 - (e.embedding <#> %s::vector) AS similarity,
    m.id AS metadata_id
FROM rag_embeddings e
JOIN rag_metadata m ON e.metadata_id = m.id
WHERE m.cabinet_id = %s
  AND (m.chunk_type = ANY(%s) OR %s IS NULL)
ORDER BY e.embedding <#> %s::vector
LIMIT %s;
```

**Проверка:**
- [ ] Поиск возвращает релевантные результаты
- [ ] Фильтрация по `cabinet_id` работает
- [ ] Фильтрация по `chunk_types` работает (если передана)
- [ ] Порог релевантности применяется
- [ ] Производительность приемлемая (< 100ms)

---

#### 3.4. Получение метаданных для результатов

**Метод:** `get_metadata_for_embeddings(embedding_ids)`

**Задачи:**
- [ ] Получить метаданные для найденных embedding IDs
- [ ] Вернуть список словарей с полями: `id`, `chunk_text`, `chunk_type`, `source_table`, `source_id`, `similarity`

**Проверка:**
- [ ] Метаданные получаются корректно
- [ ] Связь с embedding работает
- [ ] Данные структурированы правильно

---

#### 3.5. Главный метод поиска

**Метод:** `search_relevant_chunks(query_text, cabinet_id, chunk_types=None, max_chunks=5)`

**Задачи:**
- [ ] Объединить все методы:
  1. `generate_query_embedding(query_text)`
  2. `search(query_embedding, cabinet_id, chunk_types, limit=max_chunks)`
  3. `get_metadata_for_embeddings(embedding_ids)`
- [ ] Вернуть список релевантных чанков с метаданными
- [ ] Обработка ошибок (fallback на пустой список)

**Проверка:**
- [ ] End-to-end поиск работает
- [ ] Возвращаются релевантные результаты
- [ ] Ошибки обрабатываются корректно

---

### Критерии готовности Этапа 3

- ✅ Модуль векторного поиска создан
- ✅ Генерация эмбеддинга запроса работает
- ✅ Векторный поиск в pgvector работает
- ✅ Фильтрация по `cabinet_id` и `chunk_types` работает
- ✅ Порог релевантности применяется
- ✅ Метаданные получаются корректно
- ✅ End-to-end поиск работает

**Тестирование:**
- Выполнить поиск по тестовому запросу
- Проверить релевантность результатов
- Проверить производительность (< 100ms)

---

## 🎯 Этап 4: Формирование контекста

**Цель:** Объединить найденные чанки в структурированный контекст для LLM.

**Длительность:** 1-2 дня

### Задачи

#### 4.1. Создание структуры модуля

**Файл:** `gpt_integration/ai_chat/rag/context_builder.py`

**Структура модуля:**
- [ ] Создать класс `ContextBuilder`
- [ ] Метод `__init__()` — инициализация с параметрами из env
- [ ] Метод `group_by_type(chunks)` — группировка чанков по типу
- [ ] Метод `deduplicate(chunks)` — удаление дубликатов
- [ ] Метод `format_context(chunks)` — форматирование контекста
- [ ] Метод `truncate_context(context, max_length)` — обрезка контекста
- [ ] Метод `build_context(chunks)` — главный метод формирования контекста

---

#### 4.2. Группировка чанков по типу

**Метод:** `group_by_type(chunks)`

**Задачи:**
- [ ] Группировать чанки по полю `chunk_type`
- [ ] Сортировать внутри группы по `similarity` (от большего к меньшему)
- [ ] Вернуть словарь: `{chunk_type: [chunks]}`

**Проверка:**
- [ ] Группировка работает корректно
- [ ] Сортировка по similarity работает

---

#### 4.3. Дедупликация

**Метод:** `deduplicate(chunks)`

**Задачи:**
- [ ] Удалить дубликаты по `source_table` и `source_id`
- [ ] При дубликатах оставить чанк с большим `similarity`
- [ ] Вернуть список уникальных чанков

**Проверка:**
- [ ] Дубликаты удаляются
- [ ] Остается наиболее релевантный чанк

---

#### 4.4. Форматирование контекста

**Метод:** `format_context(chunks)`

**Задачи:**
- [ ] Создать структурированный текст с заголовками по типам:
  ```
  === РЕЛЕВАНТНЫЕ ДАННЫЕ ИЗ БАЗЫ ДАННЫХ ===
  
  ЗАКАЗЫ:
  - [Чанк 1]
  - [Чанк 2]
  
  ТОВАРЫ:
  - [Чанк 3]
  ...
  ```
- [ ] Использовать читаемый формат для каждого чанка
- [ ] Добавить разделители между типами

**Проверка:**
- [ ] Контекст читаемый и структурированный
- [ ] Заголовки по типам добавлены
- [ ] Форматирование корректное

---

#### 4.5. Обрезка контекста

**Метод:** `truncate_context(context, max_length)`

**Задачи:**
- [ ] Проверить длину контекста
- [ ] Если длина > `max_length` (из `RAG_CONTEXT_MAX_LENGTH`):
  - Обрезать менее релевантные чанки
  - Сохранить структуру (заголовки)
- [ ] Вернуть обрезанный контекст

**Проверка:**
- [ ] Обрезка работает корректно
- [ ] Структура сохраняется
- [ ] Наиболее релевантные чанки остаются

---

#### 4.6. Главный метод формирования контекста

**Метод:** `build_context(chunks)`

**Задачи:**
- [ ] Объединить все методы:
  1. `deduplicate(chunks)` — удалить дубликаты
  2. `group_by_type(chunks)` — сгруппировать по типам
  3. `format_context(chunks)` — отформатировать
  4. `truncate_context(context, max_length)` — обрезать при необходимости
- [ ] Вернуть финальный контекст

**Проверка:**
- [ ] End-to-end формирование контекста работает
- [ ] Контекст структурированный и читаемый
- [ ] Длина контекста в пределах лимита

---

### Критерии готовности Этапа 4

- ✅ Модуль формирования контекста создан
- ✅ Группировка по типам работает
- ✅ Дедупликация работает
- ✅ Форматирование контекста работает
- ✅ Обрезка контекста работает
- ✅ End-to-end формирование контекста работает

**Тестирование:**
- Создать тестовые чанки
- Проверить формирование контекста
- Проверить обрезку при превышении лимита

---

## 🎯 Этап 5: Интеграция с AI Chat Service

**Цель:** Интегрировать RAG модуль в существующий AI Chat Service.

**Длительность:** 2-3 дня

### Задачи

#### 5.1. Создание модуля обогащения промпта

**Файл:** `gpt_integration/ai_chat/rag/prompt_enricher.py`

**Структура модуля:**
- [ ] Создать функцию `enrich_prompt_with_rag(user_message, telegram_id, cabinet_id, original_prompt)`
- [ ] Вызвать векторный поиск через `VectorSearch`
- [ ] Вызвать формирование контекста через `ContextBuilder`
- [ ] Объединить исходный промпт с контекстом
- [ ] Вернуть обогащенный промпт

**Структура обогащенного промпта:**
```
[Исходный SYSTEM_PROMPT]

=== КОНТЕКСТ ИЗ БАЗЫ ДАННЫХ ПОЛЬЗОВАТЕЛЯ ===
[Сформированный контекст]

=== ИНСТРУКЦИИ ===
Используй данные из контекста выше для ответа на вопрос пользователя.
Если в контексте нет нужной информации, отвечай на основе общих знаний, но укажи это.
```

**Проверка:**
- [ ] Функция обогащения промпта работает
- [ ] Контекст добавляется корректно
- [ ] Fallback на исходный промпт при ошибках

---

#### 5.2. Получение cabinet_id пользователя

**Задачи:**
- [ ] Создать функцию для получения `cabinet_id` по `telegram_id`
- [ ] Запрос к основной БД: `users` → `cabinet_users` → `wb_cabinets`
- [ ] Обработка случаев: пользователь не найден, нет кабинета

**Проверка:**
- [ ] `cabinet_id` получается корректно
- [ ] Обработка edge cases работает

---

#### 5.3. Модификация AI Chat Service

**Файл:** `gpt_integration/ai_chat/app/service.py`

**Задачи:**
- [ ] Импортировать RAG модули
- [ ] В функции `send_message()`:
  - Получить `cabinet_id` пользователя
  - Проверить `RAG_ENABLED` из env
  - Если RAG включен:
    - Вызвать `enrich_prompt_with_rag()`
    - Использовать обогащенный промпт
  - Если RAG выключен или произошла ошибка:
    - Использовать исходный промпт (fallback)
- [ ] Логирование использования RAG

**Проверка:**
- [ ] Интеграция работает end-to-end
- [ ] Fallback работает при ошибках
- [ ] Логирование работает

---

#### 5.4. Обработка ошибок и fallback

**Задачи:**
- [ ] Обработка ошибок векторного поиска → fallback на обычный промпт
- [ ] Обработка ошибок получения `cabinet_id` → fallback на обычный промпт
- [ ] Обработка случаев, когда нет данных для кабинета → fallback на обычный промпт
- [ ] Логирование всех fallback случаев

**Проверка:**
- [ ] Все ошибки обрабатываются
- [ ] Fallback работает корректно
- [ ] Пользователь не видит ошибок (система работает прозрачно)

---

#### 5.5. Обновление __init__.py

**Файл:** `gpt_integration/ai_chat/rag/__init__.py`

**Задачи:**
- [ ] Экспортировать основные функции и классы:
  - `enrich_prompt_with_rag`
  - `VectorSearch`
  - `ContextBuilder`
  - `RAGIndexer`

**Проверка:**
- [ ] Импорты работают корректно

---

### Критерии готовности Этапа 5

- ✅ Модуль обогащения промпта создан
- ✅ Получение `cabinet_id` работает
- ✅ Интеграция с AI Chat Service работает
- ✅ Fallback логика работает
- ✅ Обработка ошибок работает
- ✅ End-to-end поток работает

**Тестирование:**
- Отправить запрос через Telegram бота
- Проверить, что промпт обогащается контекстом
- Проверить fallback при ошибках

---

## 🎯 Этап 6: Query Understanding (опционально)

**Цель:** Улучшить точность поиска за счет понимания намерения пользователя.

**Длительность:** 1-2 дня

### Задачи

#### 6.1. Создание модуля обработки запросов

**Файл:** `gpt_integration/ai_chat/rag/query_processor.py`

**Структура модуля:**
- [ ] Создать класс `QueryProcessor`
- [ ] Метод `classify_intent(query_text)` — классификация намерения
- [ ] Метод `extract_entities(query_text)` — извлечение сущностей
- [ ] Метод `get_chunk_types_for_intent(intent)` — определение типов чанков для намерения

---

#### 6.2. Классификация намерения

**Метод:** `classify_intent(query_text)`

**Задачи:**
- [ ] Простая классификация по ключевым словам:
  - "продаж", "выкуп", "заказ" → `sales_analytics`
  - "товар", "продукт", "карточка" → `products`
  - "остаток", "склад", "количество" → `stocks`
  - "отзыв", "рейтинг" → `reviews`
  - Иначе → `general`
- [ ] Вернуть тип намерения

**Проверка:**
- [ ] Классификация работает для тестовых запросов

---

#### 6.3. Извлечение сущностей

**Метод:** `extract_entities(query_text)`

**Задачи:**
- [ ] Поиск упоминаний `nm_id` (числа после "nm_id" или "артикул")
- [ ] Поиск дат ("сегодня", "вчера", "за неделю", конкретные даты)
- [ ] Поиск названий товаров (опционально, сложнее)
- [ ] Вернуть словарь с найденными сущностями

**Проверка:**
- [ ] Сущности извлекаются корректно

---

#### 6.4. Определение типов чанков

**Метод:** `get_chunk_types_for_intent(intent)`

**Задачи:**
- [ ] Маппинг намерений на типы чанков:
  - `sales_analytics` → `['sale', 'order']`
  - `products` → `['product', 'review']`
  - `stocks` → `['stock']`
  - `reviews` → `['review']`
  - `general` → `None` (все типы)

**Проверка:**
- [ ] Маппинг работает корректно

---

#### 6.5. Интеграция с векторным поиском

**Задачи:**
- [ ] В `VectorSearch.search()` использовать `chunk_types` из `QueryProcessor`
- [ ] Передать результат классификации в поиск

**Проверка:**
- [ ] Фильтрация по типам работает
- [ ] Поиск более точный

---

### Критерии готовности Этапа 6

- ✅ Модуль обработки запросов создан
- ✅ Классификация намерений работает
- ✅ Извлечение сущностей работает
- ✅ Определение типов чанков работает
- ✅ Интеграция с поиском работает

**Тестирование:**
- Протестировать на различных запросах
- Проверить улучшение точности поиска

---

## 🎯 Этап 7: Сервис индексации (Celery Task)

**Цель:** Создать автоматическую индексацию данных по расписанию.

**Длительность:** 1-2 дня

### Задачи

#### 7.1. Создание Celery Task

**Файл:** `server/app/tasks/rag_indexing.py` (или в основном сервисе)

**Задачи:**
- [ ] Создать Celery task `index_rag_for_cabinet(cabinet_id)`
- [ ] Вызвать `RAGIndexer.index_cabinet(cabinet_id)`
- [ ] Обработка ошибок и логирование

---

#### 7.2. Периодическая индексация

**Задачи:**
- [ ] Настроить Celery beat для запуска каждые 6 часов
- [ ] Получить список всех кабинетов
- [ ] Для каждого кабинета вызвать task
- [ ] Обработка ошибок для отдельных кабинетов

---

#### 7.3. API endpoint для ручного запуска (опционально)

**Задачи:**
- [ ] Создать endpoint `POST /api/v1/rag/index/{cabinet_id}`
- [ ] Защита API ключом
- [ ] Вызов индексации для конкретного кабинета

---

### Критерии готовности Этапа 7

- ✅ Celery task создан
- ✅ Периодическая индексация настроена
- ✅ API endpoint работает (если реализован)

**Тестирование:**
- Запустить индексацию вручную
- Проверить автоматическую индексацию

---

## 🎯 Этап 8: Тестирование и оптимизация

**Цель:** Протестировать систему и оптимизировать производительность.

**Длительность:** 2-3 дня

### Задачи

#### 8.1. Unit тесты

**Задачи:**
- [ ] Тесты для `RAGIndexer`
- [ ] Тесты для `VectorSearch`
- [ ] Тесты для `ContextBuilder`
- [ ] Тесты для `QueryProcessor` (если реализован)

---

#### 8.2. Integration тесты

**Задачи:**
- [ ] Тест полной индексации кабинета
- [ ] Тест векторного поиска
- [ ] Тест формирования контекста
- [ ] Тест интеграции с AI Chat Service

---

#### 8.3. Производительность

**Задачи:**
- [ ] Измерить время векторного поиска (цель: < 100ms)
- [ ] Измерить время индексации кабинета (цель: < 5 минут для 10K чанков)
- [ ] Оптимизировать SQL запросы
- [ ] Проверить использование индексов

---

#### 8.4. Оптимизация параметров

**Задачи:**
- [ ] Настроить `RAG_SIMILARITY_THRESHOLD` (тестирование различных значений)
- [ ] Настроить `RAG_MAX_CHUNKS` (баланс между релевантностью и размером контекста)
- [ ] Настроить `RAG_CONTEXT_MAX_LENGTH`

---

### Критерии готовности Этапа 8

- ✅ Unit тесты написаны и проходят
- ✅ Integration тесты написаны и проходят
- ✅ Производительность соответствует требованиям
- ✅ Параметры оптимизированы

---

## 🎯 Этап 9: Документация и мониторинг

**Цель:** Создать документацию и настроить мониторинг.

**Длительность:** 1 день

### Задачи

#### 9.1. Документация

**Задачи:**
- [ ] README для RAG модуля
- [ ] Описание API (если есть endpoints)
- [ ] Инструкции по настройке
- [ ] Примеры использования

---

#### 9.2. Логирование

**Задачи:**
- [ ] Логирование всех операций индексации
- [ ] Логирование использования RAG в запросах
- [ ] Логирование ошибок и fallback случаев

---

#### 9.3. Метрики

**Задачи:**
- [ ] Метрики количества проиндексированных чанков
- [ ] Метрики использования RAG (сколько запросов использовали RAG)
- [ ] Метрики производительности поиска

---

### Критерии готовности Этапа 9

- ✅ Документация создана
- ✅ Логирование настроено
- ✅ Метрики собираются

---

## 📋 Общий чек-лист готовности MVP

- [ ] Этап 1: Инфраструктура готова
- [ ] Этап 2: Индексация работает
- [ ] Этап 3: Векторный поиск работает
- [ ] Этап 4: Формирование контекста работает
- [ ] Этап 5: Интеграция с AI Chat работает
- [ ] Этап 6: Query Understanding (опционально)
- [ ] Этап 7: Автоматическая индексация работает
- [ ] Этап 8: Тестирование и оптимизация завершены
- [ ] Этап 9: Документация создана

---

## 🚀 Следующие шаги после MVP

1. **Улучшение качества поиска:**
   - Re-ranking результатов
   - Hybrid search (векторный + keyword)

2. **Расширение источников данных:**
   - Документация Wildberries
   - Внешние данные

3. **Кэширование:**
   - Кэш эмбеддингов
   - Кэш результатов поиска

4. **Аналитика:**
   - Метрики использования
   - A/B тестирование

---

**Версия:** 1.0.0  
**Дата:** 2025-01-XX  
**Статус:** Поэтапный план разработки

