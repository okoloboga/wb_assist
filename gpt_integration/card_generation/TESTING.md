# 🧪 Тестирование генерации карточек товаров

## Варианты тестирования

### 1. Ручное тестирование через Telegram бота

**Шаги:**

1. **Откройте Telegram бота** и перейдите в меню:
   - Главное меню → 🤖 AI-помощник → 🎨 Генерация карточки

2. **Шаг 1: Отправьте фото товара**
   - Нажмите кнопку "🎨 Генерация карточки"
   - Отправьте фотографию товара

3. **Шаг 2: Введите характеристики поочередно**
   - Бот запросит сначала **"1️⃣ Название товара:"** - отправьте название
   - Затем **"2️⃣ Бренд:"** - отправьте бренд
   - Затем **"3️⃣ Категория:"** - отправьте категорию
   - Каждое поле запрашивается отдельно и сохраняется сразу

4. **Шаг 3: Опишите целевую аудиторию**
   - Пример: "Женщины 25-40 лет, активные, следящие за модой"

5. **Шаг 4: Опишите уникальные преимущества**
   - Пример: "Премиальное качество материалов, уникальный дизайн"

6. **Результат:** Получите сгенерированную карточку товара

**Тестирование ошибок:**
- Отправьте текст вместо фото → должна появиться ошибка "⚠️ Пожалуйста, отправьте фотографию товара"
- Нажмите "🔙 Отмена" на любом этапе → процесс должен отмениться, состояние очищено
- Отправьте фото на этапе ввода характеристик → будет проигнорировано (ждем текст)
- Ошибка GPT API → показывается сообщение "❌ Произошла ошибка при генерации карточки"
- Ошибка сети → показывается сообщение "❌ Не удалось связаться с сервисом генерации"
- Отправьте /start на любом этапе генерации → бот полностью перезапускается и запускает стартовый сценарий

---

### 2. Автоматический тест через скрипт

**Запуск теста:**

```bash
# Из корня проекта
cd gpt_integration
python card_generation/test_card_generation.py
```

**Или через Docker:**

```bash
docker-compose exec gpt python -m gpt_integration.card_generation.test_card_generation
```

**Что проверяет:**
- ✅ Генерация промпта
- ✅ Вызов GPT API
- ✅ Форматирование ответа
- ✅ Обработка ошибок

---

### 3. Тестирование через API напрямую

**Запрос к эндпоинту:**

```bash
curl -X POST http://localhost:9000/v1/card/generate \
  -H "Content-Type: application/json" \
  -H "X-API-KEY: your_api_secret_key" \
  -d '{
    "telegram_id": 123456789,
    "photo_file_id": "test_photo_id",
    "characteristics": {
      "name": "Платье летнее",
      "brand": "Fashion Style",
      "category": "Одежда > Платья"
    },
    "target_audience": "Женщины 25-40 лет",
    "selling_points": "Качество, дизайн, комфорт"
  }'
```

**Ожидаемый ответ:**

```json
{
  "status": "success",
  "card": "ЗАГОЛОВОК ТОВАРА\n\nОПИСАНИЕ ТОВАРА\n\nКЛЮЧЕВЫЕ ХАРАКТЕРИСТИКИ\n\n..."
}
```

---

## Чек-лист тестирования

### Базовый функционал
- [ ] Кнопка "🎨 Генерация карточки" отображается в меню AI-помощника
- [ ] Начало процесса генерации работает (появляется запрос фото)
- [ ] Фото успешно сохраняется (file_id сохраняется в FSM)
- [ ] Характеристики запрашиваются поочередно:
  - [ ] Сначала запрашивается название товара
  - [ ] Затем бренд
  - [ ] Затем категория
  - [ ] Каждое поле сохраняется после ввода
- [ ] Целевая аудитория сохраняется
- [ ] Selling points сохраняются
- [ ] Карточка успешно генерируется и отправляется

### Обработка ошибок
- [ ] Текст вместо фото → показывается ошибка "⚠️ Пожалуйста, отправьте фотографию товара"
- [ ] Отмена процесса (кнопка "🔙 Отмена") → состояние очищается, возврат в меню
- [ ] Ошибка GPT API (HTTP 500) → показывается сообщение "❌ Произошла ошибка при генерации карточки"
- [ ] Ошибка сети (timeout, connection error) → показывается сообщение "❌ Не удалось связаться с сервисом генерации"
- [ ] Неверный API ключ → GPT сервис возвращает 403
- [ ] Пустые данные → проверка валидности перед отправкой в GPT

### Интеграция
- [ ] Эндпоинт `/v1/card/generate` доступен
- [ ] API ключ проверяется корректно
- [ ] Данные передаются в GPT сервис
- [ ] Ответ от GPT форматируется корректно
- [ ] Результат отправляется пользователю

---

## Отладка

### Просмотр логов

```bash
# Логи бота
docker-compose logs -f bot

# Логи GPT сервиса
docker-compose logs -f gpt

# Поиск ошибок
docker-compose logs bot gpt | grep -i error
```

### Проверка состояния FSM

Если нужно проверить состояние FSM пользователя, можно посмотреть в логах бота:
```
🎨 User {telegram_id} started card generation
📸 Photo received from user {telegram_id}
```

### Типичные проблемы

1. **Роутер не зарегистрирован**
   - Проверьте `bot/__main__.py` - должен быть `card_generation_router`

2. **Состояния не найдены**
   - Проверьте `bot/core/states.py` - должен быть `CardGenerationStates`

3. **GPT сервис недоступен**
   - Проверьте `GPT_SERVICE_URL` в переменных окружения
   - Проверьте логи: `docker-compose logs gpt`

4. **API ключ неверный**
   - Проверьте `API_SECRET_KEY` в `.env` и `docker-compose.yml`
   - Ключ должен совпадать в обоих сервисах

---

**Дата создания:** 2025-11-03

