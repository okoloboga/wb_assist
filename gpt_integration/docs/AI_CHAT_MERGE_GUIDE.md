# План консолидации AI‑чат сервиса внутри `gpt_integration`

Цель — отказаться от выделенного сервиса `ai_chat` и объединить все AI‑компоненты (чат, аналитика, генерация карточек, обработка изображений) в один процесс `gpt_integration`. Ниже последовательность действий, которая поможет провести миграцию без простоя.

---

## 1. Подготовка и аудит
1. **Сравните кодовые базы**: сверитесь, совпадает ли содержимое `ai_chat` в корне репозитория и модуль `gpt_integration/ai_chat`. Отметьте расхождения (CRUD, схемы, промпты, конфигурация).
2. **Проверка зависимостей**: выпишите, какие пакеты используются только в `ai_chat`, а какие уже есть в `gpt_integration`.

---

## 2. Выбор каноничной кодовой базы

1. **Определите источник истины**: выберите, какая версия `ai_chat` считается актуальной (исходя из production-сборки, покрытых тестов и документации).
2. **Синхронизируйте модуль**: перенесите выбранный код в `gpt_integration/ai_chat` (если потребуется — вручную объедините изменения).
3. **Перенесите документацию**: обновите файлы в `gpt_integration/ai_chat/docs` и избавьтесь от дубликатов в корне `ai_chat/docs`.
4. **Зафиксируйте решение**: сохраните в `AI_CHAT_MERGE_NOTES.md` краткое обоснование выбора каноничной версии и отличий, которые нужно сохранить для вложенного расположения.

---

## 3. Унификация зависимостей и окружения

1. **Обновите `gpt_integration/requirements.txt`**: добавьте пакеты, которые требуются `ai_chat` (SQLAlchemy, Pydantic версии, rate-limiter и т. п.).
2. **Дополните `env_example.txt`**: включите переменные из `ai_chat/env_example.txt` (например `DATABASE_URL`, `OPENAI_TEMPERATURE`, `DAILY_LIMIT`).
3. **Dockerfile**: проверьте, что образ `gpt_integration/Dockerfile` устанавливает все пакеты `ai_chat`.
4. **Удалите отдельный `ai_chat` сервис** из `docker-compose.yml` (или соответствующего manifests), оставив только `gpt_integration`.

---

## 4. Интеграция маршрутов AI‑чата

1. **Разделите приложение на роутеры**: в `gpt_integration/ai_chat/app/service.py` оформите набор `APIRouter` с эндпоинтами `/v1/chat/send`, `/v1/chat/history`, `/v1/chat/limits`, `/v1/chat/reset-limit`, `/v1/chat/stats`.
2. **Подключите роутер в основном приложении**: в `gpt_integration/service.py` добавьте `app.include_router(ai_chat_router, prefix="/v1/chat")` (или иной префикс).
3. **Унифицируйте авторизацию**: используйте проверку `X-API-KEY` (как уже реализовано для `/v1/analysis/start`).
4. **Актуализируйте OpenAPI-документацию**: убедитесь, что все маршруты отображаются в едином Swagger (`/docs`).

---

## 5. Работа с базой данных

1. **Конфигурация SQLAlchemy**: уберите дублирующую инициализацию, используйте единый `SessionLocal` (из `gpt_integration/ai_chat/app/database.py` или объединённого модуля).
2. **Миграции**: решите, нужны ли Alembic-скрипты; при отсутствии — оставьте автосоздание таблиц, но добавьте инициализацию при старте приложения.
3. **Переменные окружения**: выставьте `DATABASE_URL` в едином `.env` и в инфраструктуре (prod/stage).
4. **Мониторинг сброса лимитов**: при переносе убедитесь, что CRON/админ-функции продолжают работать (если есть отдельные сервисы или задания).

---

## 6. Гармонизация общих компонентов

1. **Промпты**: проверьте, что системные промпты для чата теперь подчиняются единому управлению (можно хранить в `gpt_integration/ai_chat/app/prompts.py`).
2. **Логирование**: унифицируйте формат логов через общий конфиг `logging`.
3. **Ошибки и исключения**: приведите обработку ошибок к стандарту `gpt_integration` (HTTPException, коды статусов, сообщения).
4. **Rate limiting**: оставьте актуальный механизм (через БД); при необходимости добавьте дополнительную защиту (Redis, in-memory) в будущем.

---

## 7. Тестирование

1. **Перенесите тесты**: переместите `ai_chat/tests` в `gpt_integration/ai_chat/tests`, обновите импорты (учитывайте новый путь модулей).
2. **Запуск pytest**: выполняйте тесты из корня репозитория, чтобы `bot/utils` попадали в `PYTHONPATH` (пример: `PYTHONPATH=$(pwd) pytest gpt_integration/ai_chat/tests` или настройте переменную окружения перед запуском).
3. **Интеграционные тесты**: добавьте проверки, что все эндпоинты доступны через один сервис (чаты, аналитика, генерация карточек, изображения).
4. **Покрытие**: обновите отчёты (Coverage, htmlcov), проверьте, что не потеряны критические проверки.

---

## 8. Обновление документации ✅

1. ✅ **`README.md` в корне `gpt_integration`**: дополнен раздел «Возможности» пунктами про чат и лимиты.
2. ✅ **Архитектурные схемы**: добавлены Mermaid-диаграммы, показывающие единый сервис и его модули (`ai_chat`, `analysis`, `card_generation`, `photo_processing`).
3. ✅ **FAQ/траблшутинг**: перенесены подсказки из `ai_chat/README.md` (лимиты, ошибки OpenAI, сброс ключей).
4. ✅ **Удалены дублирующие файлы**: корневая папка `ai_chat` удалена, так как весь функционал перенесен в `gpt_integration/ai_chat/`.

---

## 9. Финальные проверки и деплой

### Конфигурация ✅

1. ✅ **docker-compose.yml**: добавлена переменная `DATABASE_URL` для сервиса `gpt`
2. ✅ **docker-compose.yml**: добавлена зависимость `depends_on: db` для сервиса `gpt`
3. ✅ **docker-compose.yml**: добавлена переменная `DAILY_LIMIT` для настройки лимитов
4. ✅ **docker-compose.yml**: исправлена проблема с `OPENAI_BASE_URL` (убрана пустая строка)
5. ✅ **Dockerfile**: корректно настроен для объединенного сервиса
6. ✅ **requirements.txt**: включены все зависимости для AI Chat (SQLAlchemy, psycopg2, asyncpg)
7. ✅ **.env**: удалена пустая переменная `OPENAI_BASE_URL`

### Smoke-тест ✅

Созданы автоматизированные скрипты для проверки всех эндпоинтов:

**PowerShell (Windows):**
```powershell
$env:API_SECRET_KEY = "CnWvwoDwwGKh"
.\gpt_integration\smoke_test.ps1
```

**Bash (Linux/Mac):**
```bash
export API_SECRET_KEY="CnWvwoDwwGKh"
./gpt_integration/smoke_test.sh
```

Подробная документация: `gpt_integration/SMOKE_TEST_README.md`

Для локального тестирования:
```bash
# 1. Убедитесь, что PostgreSQL запущен
docker-compose up -d db

# 2. Установите переменные окружения
export DATABASE_URL="postgresql://user:NGZVLhEBR2i5@localhost:5432/wb_assist_db"
export OPENAI_API_KEY="your-key"
export API_SECRET_KEY="CnWvwoDwwGKh"

# 3. Запустите сервис
python -m gpt_integration.service

# 4. Запустите smoke test
./gpt_integration/smoke_test.sh
```

### Docker Compose тест ⏳

```bash
# Пересоберите и запустите
docker-compose up -d --build gpt

# Проверьте логи
docker-compose logs -f gpt

# Проверьте здоровье
curl http://localhost:9000/health

# Проверьте AI Chat эндпоинты
curl -X GET "http://localhost:9000/v1/chat/limits/123456789" \
  -H "X-API-KEY: CnWvwoDwwGKh"
```

### Чеклист проверок ✅

- [x] **Health check**: `/health` возвращает `{"status": "ok"}`
- [x] **AI Chat**: `/v1/chat/limits/{telegram_id}` работает
- [x] **AI Chat**: `/v1/chat/send` принимает запросы и возвращает ответы от OpenAI
- [ ] **Analysis**: `/v1/analysis/start` работает (требует тестирования)
- [x] **Database**: таблицы `ai_chat_requests` и `ai_chat_daily_limits` созданы
- [x] **Logs**: сервис логирует "✅ Database tables created/verified"
- [x] **Logs**: сервис логирует "🎯 AI Chat components ready"
- [x] **OpenAI**: успешное подключение к OpenAI API

### CI/CD ⏳

1. **Pipeline**: убедитесь, что Pipeline собирает единый образ `gpt_integration`
2. **Tests**: прогоните тесты для всех модулей
3. **Build**: проверьте успешную сборку Docker образа

### Инфраструктура ⏳

1. **Переменные окружения**: обновите на prod/stage:
   - `DATABASE_URL` - для AI Chat
   - `DAILY_LIMIT` - лимит запросов (по умолчанию 30)
   - Все переменные OpenAI
2. **Секреты**: убедитесь, что `API_SECRET_KEY` и `OPENAI_API_KEY` настроены
3. **Сетевые правила**: проверьте доступность порта 9000

### Мониторинг и алерты ⏳

1. **Дашборды**: обновите для единого сервиса `gpt_integration`
2. **Метрики**: добавьте мониторинг AI Chat эндпоинтов
3. **Алерты**: настройте алерты на ошибки rate limiting и OpenAI API
4. **Логи**: централизуйте логи всех модулей

### Оптимизация ресурсов ⏳

1. **Ресурсы**: пересмотрите CPU/Memory лимиты (теперь один процесс)
2. **Масштабирование**: настройте горизонтальное масштабирование при необходимости
3. **Connection pooling**: проверьте настройки пула соединений к БД

---

## 10. Пост-мортем и поддержка

1. **Ретроспектива**: проведите разбор внедрения, зафиксируйте уроки и новые риски.
2. **План обслуживания**: описывайте, как обновлять отдельные модули (чат, анализ, генерация карточек, изображений) внутри монорепозитория.
3. **Резервный план**: подготовьте fallback (например, возможность временно вернуть отдельный контейнер, если появится критический баг).

После выполнения этих шагов все AI‑подсервисы будут обслуживаться одним приложением `gpt_integration`, что упростит конфигурацию, деплой и поддержку.

