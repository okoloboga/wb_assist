# 🧪 Инструкции по тестированию Photo Processing Service

## 📋 Содержание

1. [Подготовка к тестированию](#подготовка-к-тестированию)
2. [Тестирование полного сценария](#тестирование-полного-сценария)
3. [Тестирование обработки ошибок](#тестирование-обработки-ошибок)
4. [Тестирование отмены](#тестирование-отмены)
5. [Тестирование сохранения и истории](#тестирование-сохранения-и-истории)
6. [Чек-лист тестирования](#чек-лист-тестирования)

---

## 🔧 Подготовка к тестированию

### 1. Проверка переменных окружения

Убедитесь, что в `.env` файле установлены следующие переменные:

```env
# Image Generation API Configuration
IMAGE_GEN_API_KEY=your_api_key_here
IMAGE_GEN_BASE_URL=https://api.example.com/v1
IMAGE_GEN_TIMEOUT=60
IMAGE_GEN_MAX_RETRIES=3
PHOTO_STORAGE_TYPE=url

# Bot Token
BOT_TOKEN=your_bot_token_here

# API Secret Key
API_SECRET_KEY=your_secret_key_here
```

### 2. Запуск сервисов

```bash
# Запуск через Docker Compose
docker-compose up -d

# Проверка логов
docker-compose logs -f gpt
docker-compose logs -f bot
```

### 3. Проверка инициализации БД

Проверьте логи GPT сервиса на наличие сообщения:
```
✅ Photo processing database initialized
```

---

## ✅ Тестирование полного сценария

### Сценарий: Успешная обработка фото

**Шаги:**

1. **Открыть бота в Telegram**
   - Найти бота по username
   - Нажать `/start`

2. **Перейти в меню AI-помощник**
   - Нажать кнопку "🤖 AI-помощник"
   - Убедиться, что отображается кнопка "📸 Обработка фото"

3. **Начать процесс обработки**
   - Нажать "📸 Обработка фото"
   - Проверить сообщение: "📸 Обработка фотографии... Шаг 1: Пришлите фото"
   - Проверить наличие кнопки "🔙 Отмена"

4. **Отправить фото**
   - Отправить любую фотографию
   - Проверить сообщение: "✅ Фото получено! Шаг 2: Опишите, что нужно сделать"
   - Проверить примеры промптов

5. **Отправить промпт**
   - Отправить описание, например: "Сделай фон более ярким"
   - Проверить сообщение: "⏳ Обрабатываю фотографию..."

6. **Получить результат**
   - Дождаться обработки
   - Проверить получение обработанного фото
   - Проверить сообщение: "✅ Фото обработано! Результат сохранен в вашей галерее"

**Ожидаемый результат:**
- ✅ Все состояния FSM работают корректно
- ✅ Фото успешно обработано
- ✅ Результат сохранен в БД
- ✅ Пользователь получил обработанное изображение

---

## ❌ Тестирование обработки ошибок

### Тест 1: Некорректный ввод (не фото)

**Шаги:**
1. Начать процесс обработки
2. Вместо фото отправить текст или документ

**Ожидаемый результат:**
- ⚠️ Сообщение: "Пожалуйста, отправьте фотографию"
- Состояние FSM не изменилось

### Тест 2: Слишком короткий промпт

**Шаги:**
1. Начать процесс обработки
2. Отправить фото
3. Отправить промпт из 1-2 символов (например: "ok")

**Ожидаемый результат:**
- ⚠️ Сообщение: "Описание слишком короткое. Пожалуйста, опишите подробнее (минимум 3 символа)"
- Состояние FSM не изменилось

### Тест 3: Слишком длинный промпт

**Шаги:**
1. Начать процесс обработки
2. Отправить фото
3. Отправить промпт длиннее 1000 символов

**Ожидаемый результат:**
- ⚠️ Сообщение: "Описание слишком длинное. Пожалуйста, сократите до 1000 символов"
- Состояние FSM не изменилось

### Тест 4: Ошибка API (если API недоступен)

**Шаги:**
1. Временно установить неверный `IMAGE_GEN_API_KEY` или `IMAGE_GEN_BASE_URL`
2. Начать процесс обработки
3. Отправить фото и промпт

**Ожидаемый результат:**
- ❌ Сообщение об ошибке (например: "Сервис временно недоступен")
- Состояние FSM очищено
- Пользователь вернулся в меню

### Тест 5: Таймаут обработки

**Шаги:**
1. Установить очень маленький `IMAGE_GEN_TIMEOUT` (например, 1 секунду)
2. Начать процесс обработки
3. Отправить фото и промпт

**Ожидаемый результат:**
- ❌ Сообщение: "Превышено время ожидания. Попробуйте еще раз"
- Retry логика отработала (3 попытки)
- Состояние FSM очищено

---

## 🔙 Тестирование отмены

### Тест 1: Отмена на этапе ожидания фото

**Шаги:**
1. Начать процесс обработки
2. Нажать кнопку "🔙 Отмена"

**Ожидаемый результат:**
- ✅ Сообщение: "Обработка фото отменена"
- Состояние FSM очищено
- Пользователь вернулся в меню AI-помощник

### Тест 2: Отмена на этапе ожидания промпта

**Шаги:**
1. Начать процесс обработки
2. Отправить фото
3. Нажать кнопку "🔙 Отмена"

**Ожидаемый результат:**
- ✅ Сообщение: "Обработка фото отменена"
- Состояние FSM очищено
- Пользователь вернулся в меню AI-помощник

### Тест 3: Команда /start во время процесса

**Шаги:**
1. Начать процесс обработки
2. Отправить команду `/start`

**Ожидаемый результат:**
- Состояние FSM очищено
- Пользователь перенаправлен на стандартную регистрацию

---

## 💾 Тестирование сохранения и истории

### Тест 1: Сохранение результата в БД

**Шаги:**
1. Успешно обработать фото
2. Проверить логи GPT сервиса на наличие сообщения:
   ```
   ✅ Result saved with ID: <id>
   ```
3. Проверить БД напрямую:
   ```sql
   SELECT * FROM photo_processing_results ORDER BY created_at DESC LIMIT 1;
   ```

**Ожидаемый результат:**
- ✅ Запись создана в таблице `photo_processing_results`
- ✅ Поле `result_photo_url` содержит URL
- ✅ Все метаданные сохранены корректно

### Тест 2: Получение истории через API

**Шаги:**
1. Обработать несколько фото (минимум 3)
2. Выполнить запрос к API:
   ```bash
   curl -X GET "http://localhost:9000/v1/photo/history/<telegram_id>?limit=2&offset=0" \
     -H "X-API-KEY: your_secret_key"
   ```

**Ожидаемый результат:**
- ✅ Возвращается список из 2 записей (limit=2)
- ✅ Записи отсортированы по дате (новые первыми)
- ✅ Поле `total` содержит общее количество записей
- ✅ Пагинация работает корректно

### Тест 3: Пагинация истории

**Шаги:**
1. Обработать 5 фото
2. Запросить первую страницу: `limit=2&offset=0`
3. Запросить вторую страницу: `limit=2&offset=2`
4. Запросить третью страницу: `limit=2&offset=4`

**Ожидаемый результат:**
- ✅ Первая страница: 2 записи (самые новые)
- ✅ Вторая страница: 2 записи (следующие)
- ✅ Третья страница: 1 запись (последняя)
- ✅ Нет дубликатов между страницами

---

## 📝 Чек-лист тестирования

### Основной функционал
- [ ] Кнопка "📸 Обработка фото" отображается в меню
- [ ] Процесс начинается корректно
- [ ] Фото принимается и сохраняется в FSM
- [ ] Промпт валидируется (3-1000 символов)
- [ ] Запрос отправляется в GPT сервис
- [ ] Обработанное фото возвращается пользователю
- [ ] Результат сохраняется в БД

### Обработка ошибок
- [ ] Некорректный ввод (не фото) обрабатывается
- [ ] Короткий промпт отклоняется
- [ ] Длинный промпт отклоняется
- [ ] Ошибки API обрабатываются корректно
- [ ] Таймауты обрабатываются с retry логикой
- [ ] Сетевые ошибки обрабатываются

### Отмена процесса
- [ ] Отмена на этапе ожидания фото работает
- [ ] Отмена на этапе ожидания промпта работает
- [ ] Команда /start очищает состояние FSM
- [ ] Состояние корректно очищается при ошибках

### База данных
- [ ] Таблица создается при старте сервиса
- [ ] Результаты сохраняются корректно
- [ ] URL обработанного изображения сохраняется
- [ ] Метаданные сохраняются полностью
- [ ] Индексы работают (быстрый поиск по telegram_id)

### История обработок
- [ ] API эндпоинт `/v1/photo/history/{telegram_id}` работает
- [ ] Пагинация работает корректно
- [ ] Сортировка по дате (новые первыми)
- [ ] Фильтрация по telegram_id
- [ ] Поле `total` возвращает правильное количество

### Интеграция
- [ ] Роутер зарегистрирован в bot/__main__.py
- [ ] Эндпоинты зарегистрированы в gpt_integration/service.py
- [ ] БД инициализируется при старте GPT сервиса
- [ ] Переменные окружения загружаются корректно
- [ ] Docker Compose конфигурация корректна

---

## 🐛 Известные проблемы и решения

### Проблема: БД не инициализируется

**Решение:**
1. Проверить логи GPT сервиса: `docker-compose logs gpt`
2. Убедиться, что `DATABASE_URL` установлен корректно
3. Перезапустить сервис: `docker-compose restart gpt`

### Проблема: API ключ не работает

**Решение:**
1. Проверить `.env` файл на наличие `IMAGE_GEN_API_KEY`
2. Убедиться, что переменная передается в docker-compose.yml
3. Перезапустить сервисы: `docker-compose up -d`

### Проблема: Таймаут при обработке

**Решение:**
1. Увеличить `IMAGE_GEN_TIMEOUT` в `.env` (например, до 120 секунд)
2. Проверить доступность API сервиса генерации изображений
3. Проверить сетевое подключение

---

## 📊 Метрики успешности

После завершения тестирования убедитесь, что:

- ✅ **100%** основных сценариев работают корректно
- ✅ **100%** ошибок обрабатываются с понятными сообщениями
- ✅ **0** критических ошибок в логах
- ✅ **< 5 секунд** среднее время обработки (зависит от API)
- ✅ **100%** результатов сохраняются в БД

---

## 🎯 Следующие шаги

После успешного тестирования:

1. ✅ Получить реальный API ключ для генерации изображений
2. ✅ Протестировать с реальным API
3. ✅ Настроить мониторинг и алерты
4. ✅ Добавить rate limiting (если необходимо)
5. ✅ Оптимизировать производительность
6. ✅ Развернуть в production

---

**Версия:** 1.0.0  
**Дата:** 2025-01-29  
**Статус:** 🟢 Готово к тестированию
