# Макет дашборда

Это визуальный прототип (mockup) аналитического дашборда с фейковыми данными.

## Что реализовано

✅ **Визуальные компоненты:**
- Header с заголовком и временем обновления
- Переключатель временных периодов (1 месяц / 2 месяца / 3 месяца / 6 месяцев)
- Кнопка "Выбрать период" с календарём для произвольного диапазона дат
- 4 карточки метрик (Заказы, Выкупы, Отмены, Возвраты)
- Единый линейный график со всеми метриками на одной плоскости
- Возможность включения/отключения отображения каждой линии
- Таблица состояния складов с фильтрами:
  - Поиск по номенклатуре
  - Фильтр по складу
  - Фильтр по размеру
  - Кнопка сброса всех фильтров
  - Адаптивная для desktop/mobile

✅ **Адаптивный дизайн:**
- Mobile-first подход
- Breakpoints: 320px - 1920px
- Мобильная версия таблицы (карточки)
- Адаптивная сетка для метрик

✅ **Минималистичный стиль:**
- Tailwind CSS
- Единая цветовая схема
- Иконки Lucide React
- Высокая контрастность

## Запуск макета

1. Установите зависимости:
```bash
cd dashboard
npm install
```

2. Запустите dev сервер:
```bash
npm run dev
```

3. Откройте в браузере:
```
http://localhost:3000
```

## Структура компонентов

```
src/
├── components/
│   ├── layout/
│   │   ├── Header.tsx           # Шапка дашборда
│   │   └── Container.tsx        # Контейнер для контента
│   ├── common/
│   │   └── TimePeriodSelector.tsx  # Переключатель периодов
│   ├── charts/
│   │   ├── MetricCard.tsx       # Карточка метрики
│   │   └── OrdersChart.tsx      # График заказов
│   └── warehouse/
│       └── WarehouseTable.tsx   # Таблица складов
├── utils/
│   └── mockData.ts              # Фейковые данные
└── App.tsx                      # Главный компонент
```

## Mock данные

Все данные захардкожены в `src/utils/mockData.ts`:
- `mockMetrics` - агрегированные метрики
- `mockChartDataByPeriod` - данные для графиков (генерируются для каждого периода)
  - 1 месяц: 4 точки
  - 2 месяца: 8 точек
  - 3 месяца: 12 точек
  - 6 месяцев: 24 точки
- `mockWarehouseData` - 10 товаров на складах

## Что НЕ реализовано

❌ API интеграция
❌ React Query для данных
❌ Реальная фильтрация по периодам
❌ Загрузка данных с сервера
❌ Обработка ошибок
❌ Тесты

## Следующие шаги

После утверждения макета:
1. Интегрировать с реальным API
2. Добавить React Query
3. Реализовать фильтрацию по периодам
4. Добавить состояния загрузки
5. Обработка ошибок
6. Тесты

## Скриншоты

Откройте дашборд в браузере чтобы увидеть:
- 📊 Графики продаж вверху
- 📦 Таблицу складов внизу
- 📱 Адаптивность на мобильных
- 🎨 Минималистичный дизайн
