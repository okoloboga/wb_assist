# Очистка кэша браузера для просмотра изменений

## ✅ Docker контейнеры перезапущены
- Dashboard пересобран с флагом `--no-cache`
- Все контейнеры запущены и работают
- Dashboard доступен на: **http://localhost:3000**

## 🔄 Как очистить кэш браузера

### Google Chrome / Microsoft Edge
1. Откройте дашборд: http://localhost:3000
2. Нажмите **Ctrl + Shift + Delete** (или **Cmd + Shift + Delete** на Mac)
3. Выберите:
   - ✅ Изображения и файлы, сохраненные в кеше
   - ✅ Файлы cookie и другие данные сайтов
4. Временной диапазон: **Все время**
5. Нажмите **Удалить данные**

### Или используйте жесткую перезагрузку
1. Откройте дашборд: http://localhost:3000
2. Откройте DevTools (F12)
3. **Правой кнопкой** на кнопку обновления (⟳)
4. Выберите **Очистить кэш и жесткая перезагрузка**

### Firefox
1. Откройте дашборд: http://localhost:3000
2. Нажмите **Ctrl + Shift + Delete**
3. Выберите:
   - ✅ Кэш
   - ✅ Куки
4. Временной диапазон: **Все**
5. Нажмите **Удалить сейчас**

### Safari
1. Откройте дашборд: http://localhost:3000
2. Меню **Safari** → **Настройки** → **Дополнения**
3. Включите **Показывать меню "Разработка"**
4. Меню **Разработка** → **Очистить кэш**
5. Перезагрузите страницу (Cmd + R)

## 🎯 Что проверить после очистки кэша

### 1. Откройте дашборд
```
http://localhost:3000
```

### 2. Откройте консоль браузера (F12)
Проверьте, что нет ошибок:
- ✅ Нет ошибок `Cannot read property 'daily_data'`
- ✅ Нет ошибок `Cannot read property 'purchases'`

### 3. Проверьте Network вкладку
Убедитесь, что запросы идут к API:
- ✅ `/api/v1/bot/analytics/summary` - возвращает данные
- ✅ `/api/v1/bot/analytics/daily-trends` - возвращает данные

### 4. Проверьте график
- ✅ График "Динамика метрик" отображается
- ✅ Видны 4 линии (заказы, выкупы, отмены, возвраты)
- ✅ Кнопки переключения метрик работают

### 5. Проверьте карточки метрик
- ✅ Заказы - показывает число
- ✅ Выкупы - показывает число
- ✅ Отмены - показывает число
- ✅ Возвраты - показывает число

## 🐛 Если график все еще не работает

### 1. Проверьте версию файлов
Откройте DevTools → Sources → найдите `App.tsx`:
```typescript
// Должно быть:
if (!dailyTrendsData?.time_series) {
  return []
}
```

Если видите старый код (`daily_data`), значит кэш не очистился.

### 2. Принудительная очистка
```bash
# В браузере откройте:
chrome://settings/clearBrowserData

# Или используйте режим инкогнито:
Ctrl + Shift + N (Chrome)
Ctrl + Shift + P (Firefox)
```

### 3. Проверьте Docker
```bash
# Убедитесь, что dashboard пересобран
docker compose ps

# Должно быть:
# wb_assist-dashboard-1   Up X seconds   0.0.0.0:3000->80/tcp
```

### 4. Проверьте логи dashboard
```bash
docker compose logs dashboard
```

## 📝 Дата обновления
27 ноября 2025, 12:58
