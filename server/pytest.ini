[tool:pytest]
asyncio_mode = auto
timeout = 30
markers =
    timeout: mark test as having a timeout
    slow: mark test as slow running
    integration: mark test as integration test
    unit: mark test as unit test