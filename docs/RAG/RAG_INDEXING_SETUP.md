# RAG Indexing Setup - Автоматическая индексация через Celery

## 📋 Обзор

Автоматическая индексация данных кабинетов в векторную БД для улучшения ответов AI-ассистента.

**Периодичность:** Каждые 6 часов (настраивается)

**Архитектура:**
- API endpoint в AI-сервисе (`/v1/rag/index/{cabinet_id}`)
- Celery tasks в основном сервисе
- Отдельная очередь `rag_queue` для индексации

---

## ⚙️ Environment Variables

Добавьте в `.env` файл следующие переменные (если их еще нет):

```bash
# RAG Configuration - Индексация
RAG_INDEXING_INTERVAL_HOURS=6  # Интервал индексации (в часах)
RAG_ENABLED=true                # Включить RAG
RAG_EMBEDDING_BATCH_SIZE=100    # Размер батча для генерации эмбеддингов

# RAG Configuration - Поиск
RAG_MAX_CHUNKS=5                # Максимум чанков в контексте
RAG_SIMILARITY_THRESHOLD=0.5    # Порог схожести для поиска (0.0-1.0)
RAG_CONTEXT_MAX_LENGTH=3000     # Максимальная длина контекста

# RAG Configuration - База данных
RAG_VECTOR_DB_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}

# OpenAI - Эмбеддинги
OPENAI_EMBEDDINGS_MODEL=text-embedding-3-small  # Модель для эмбеддингов
OPENAI_API_KEY=your_openai_api_key              # API ключ OpenAI
```

### Описание переменных:

#### Индексация:

- **`RAG_INDEXING_INTERVAL_HOURS`** (по умолчанию: `6`)
  - Интервал между автоматическими индексациями
  - Рекомендуемые значения: 4-12 часов
  - Слишком частая индексация = лишние затраты на API OpenAI
  - Слишком редкая = устаревшие данные в RAG

- **`RAG_EMBEDDING_BATCH_SIZE`** (по умолчанию: `100`)
  - Количество чанков в одном запросе к OpenAI API
  - Оптимальное значение: 50-200
  - Больше = меньше запросов, но больше времени на запрос

#### Поиск:

- **`RAG_MAX_CHUNKS`** (по умолчанию: `5`)
  - Максимальное количество чанков в контексте для AI
  - Рекомендуемые значения: 3-10
  - Больше = более полный контекст, но дороже токены

- **`RAG_SIMILARITY_THRESHOLD`** (по умолчанию: `0.3`)
  - Минимальное косинусное сходство для включения чанка (inner product для нормализованных векторов)
  - Диапазон: 0.0 (нет релевантности) - 1.0 (идеальное совпадение)
  - Интерпретация: 0.7-1.0 (высокая), 0.4-0.7 (средняя), 0.0-0.4 (низкая)
  - Оптимальное значение: 0.3-0.5 (баланс между precision и recall)

- **`RAG_CONTEXT_MAX_LENGTH`** (по умолчанию: `3000`)
  - Максимальная длина контекста в символах
  - Ограничивает токены, отправляемые в AI

---

## 🚀 Запуск и тестирование

### 1. Проверка конфигурации

Убедитесь, что все переменные окружения установлены:

```bash
# В контейнере celery-worker
docker-compose exec celery-worker env | grep RAG

# Должны быть:
# RAG_INDEXING_INTERVAL_HOURS=6
```

### 2. Перезапуск сервисов

После изменения конфигурации перезапустите сервисы:

```bash
# Перезапуск только Celery сервисов
docker-compose restart celery-worker celery-beat

# Полный перезапуск (если изменили docker-compose.yml)
docker-compose down
docker-compose up -d
```

### 3. Проверка логов

Следите за логами для проверки работы:

```bash
# Логи Celery Beat (планировщик)
docker-compose logs -f celery-beat

# Логи Celery Worker (выполнение задач)
docker-compose logs -f celery-worker

# Логи AI-сервиса (индексация)
docker-compose logs -f gpt
```

Вы должны увидеть сообщения:
```
✅ RAG configuration:
   Indexing interval: 6 hours
```

### 4. Ручной запуск индексации

Для тестирования запустите индексацию вручную:

```bash
# Войти в контейнер основного сервиса
docker-compose exec server python

# В Python shell:
from app.features.rag.tasks import index_rag_for_cabinet
result = index_rag_for_cabinet.delay(1)  # Замените 1 на ID кабинета
print(result.get())  # Ждать результата
```

Или через API напрямую:

```bash
curl -X POST \
  http://localhost:9000/v1/rag/index/1 \
  -H "X-API-KEY: your_api_key_here"
```

### 5. Проверка статуса индексации

Проверить статус индексации:

```bash
curl -X GET \
  http://localhost:9000/v1/rag/status/1 \
  -H "X-API-KEY: your_api_key_here"
```

Ответ:
```json
{
  "status": "success",
  "cabinet_id": 1,
  "indexing_status": "completed",
  "last_indexed_at": "2025-12-16T15:30:00",
  "total_chunks": 2250,
  "updated_at": "2025-12-16T15:35:00"
}
```

---

## 📊 Мониторинг

### Celery Beat Schedule

Проверить расписание задач:

```bash
# В контейнере celery-beat
docker-compose exec celery-beat ls -la /app/beat_data/

# Должен быть файл: celerybeat-schedule
```

### Celery Worker Queues

Проверить, что worker обрабатывает очередь `rag_queue`:

```bash
docker-compose logs celery-worker | grep rag_queue

# Должно быть:
# --queues=sync_queue,alerts_queue,export_queue,digest_queue,scraping_queue,rag_queue
```

### База данных

Проверить таблицы RAG в БД:

```sql
-- Подключиться к БД
docker-compose exec db psql -U your_user -d your_database

-- Проверить таблицы RAG
\dt rag_*

-- Должны быть:
-- rag_metadata
-- rag_embeddings
-- rag_index_status

-- Проверить данные индексации
SELECT cabinet_id, indexing_status, last_indexed_at, total_chunks
FROM rag_index_status;
```

---

## 🔧 Troubleshooting

### Проблема: Задача не запускается по расписанию

**Решение:**
1. Проверьте логи `celery-beat`:
   ```bash
   docker-compose logs celery-beat | grep "index-all-cabinets-rag"
   ```
2. Убедитесь, что `RAG_INDEXING_INTERVAL_HOURS` установлен в `celery-beat`
3. Перезапустите `celery-beat`:
   ```bash
   docker-compose restart celery-beat
   ```

### Проблема: Timeout при индексации

**Причина:** Генерация эмбеддингов занимает много времени

**Решение:**
1. Уменьшите `RAG_EMBEDDING_BATCH_SIZE` (например, с 100 до 50)
2. Увеличьте timeout в `tasks.py` (по умолчанию 600 секунд)

### Проблема: Ошибка "GPT_INTEGRATION_URL not set"

**Решение:**
1. Проверьте, что `GPT_INTEGRATION_URL=http://gpt:9000` установлен в `celery-worker`
2. Перезапустите `celery-worker`:
   ```bash
   docker-compose restart celery-worker
   ```

### Проблема: API возвращает 403 (Forbidden)

**Причина:** Неверный API ключ

**Решение:**
1. Проверьте, что `API_SECRET_KEY` одинаковый в `server` и `gpt` сервисах
2. Проверьте в docker-compose.yml:
   ```yaml
   server:
     environment:
       - API_SECRET_KEY=${API_SECRET_KEY}
   gpt:
     environment:
       - API_SECRET_KEY=${API_SECRET_KEY}
   ```

---

## 📈 Оптимизация производительности

### Для небольших кабинетов (< 100 товаров):
```bash
RAG_INDEXING_INTERVAL_HOURS=12  # Реже индексировать
RAG_EMBEDDING_BATCH_SIZE=50     # Меньше батчи
```

### Для больших кабинетов (> 500 товаров):
```bash
RAG_INDEXING_INTERVAL_HOURS=6   # Чаще обновлять
RAG_EMBEDDING_BATCH_SIZE=100    # Больше батчи для скорости
```

### Для экономии API токенов:
```bash
RAG_INDEXING_INTERVAL_HOURS=24  # Раз в день
RAG_MAX_CHUNKS=3                # Меньше контекста
```

---

## ✅ Checklist готовности

- [ ] Все переменные окружения установлены в `.env`
- [ ] `docker-compose.yml` обновлен (добавлен `rag_queue`)
- [ ] Сервисы перезапущены (`docker-compose restart`)
- [ ] Таблицы RAG созданы в БД (`rag_metadata`, `rag_embeddings`, `rag_index_status`)
- [ ] Celery beat показывает расписание для `index-all-cabinets-rag`
- [ ] Celery worker обрабатывает очередь `rag_queue`
- [ ] Ручной запуск индексации работает
- [ ] Логи показывают успешную индексацию

---

**Версия:** 1.0.0
**Дата:** 2025-12-16
**Статус:** Этап 7 - Автоматическая индексация через Celery
