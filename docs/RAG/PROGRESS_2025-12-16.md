# Event-Driven RAG Indexing - Progress Report

**Дата:** 2025-12-16
**Статус:** ✅ Этап 2 в процессе (70% завершено)

---

## 📋 Краткое резюме

Реализована Event-driven архитектура для RAG индексации вместо независимого расписания.

**Ключевое решение:** RAG индексация триггерится после каждой успешной синхронизации WB API, используя готовую дельту изменений.

---

## ✅ Что завершено (Этап 2: 70%)

### 1. Документация для Event-driven подхода ✅

**Обновлены документы:**
- `INCREMENTAL_INDEXING_PLAN.md` - изменена архитектура с "Гибридной" на "Event-driven"
- `STAGE1_API_SPECIFICATION.md` - обновлены Celery tasks и Beat schedule
- `EVENT_DRIVEN_ARCHITECTURE.md` - **НОВЫЙ** подробный документ с архитектурой

**Ключевые изменения в документации:**
- Инкрементальная индексация: триггер после WB sync (не по расписанию)
- Celery Beat schedule: только weekly full rebuild (воскресенье 03:00)
- API: добавлен параметр `changed_ids` для передачи дельты

### 2. Модель RAGMetadata ✅

**Файл:** `gpt_integration/ai_chat/RAG/models.py:34`

**Добавлено:**
```python
chunk_hash = Column(String(64), nullable=True, index=True)
```

Используется для hash-based change detection - пропускаем генерацию эмбеддинга если chunk_text не изменился.

### 3. Миграции БД ✅

**RAG БД (PostgreSQL):**
- `gpt_integration/ai_chat/RAG/migrations/001_add_chunk_hash.sql` - добавление chunk_hash
- `gpt_integration/ai_chat/RAG/migrations/001_populate_chunk_hash.py` - заполнение для существующих записей

**Основная БД (Alembic):**
- `server/app/alembic/versions/009_add_timestamp_indexes_for_rag.py`
- **20 новых индексов** для оптимизации инкрементальных запросов:
  - `created_at`, `updated_at` для каждой таблицы (orders, products, stocks, reviews, sales)
  - Составные индексы `(cabinet_id, created_at)`, `(cabinet_id, updated_at)`

### 4. Hash-based Change Detection ✅

**Файл:** `gpt_integration/ai_chat/RAG/indexer.py:72`

**Реализовано:**
```python
@staticmethod
def calculate_chunk_hash(chunk_text: str) -> str:
    """SHA256 hash от chunk_text для оптимизации."""
    return hashlib.sha256(chunk_text.encode('utf-8')).hexdigest()
```

Интегрировано в `save_to_vector_db()` - hash вычисляется при создании и обновлении записей.

### 5. Extract by IDs (Event-driven) ✅

**Файл:** `gpt_integration/ai_chat/RAG/indexer.py:183`

**Реализовано:**
```python
async def extract_data_by_ids(
    self,
    cabinet_id: int,
    changed_ids: Dict[str, List[int]]
) -> Dict[str, List[Dict[str, Any]]]:
    """
    Извлечение данных по списку ID (Event-driven indexing).

    Использует IN queries вместо timestamp ranges - гораздо быстрее.
    """
```

**Преимущества:**
- Indexed lookup (IN queries) vs timestamp range scans
- Нет риска пропустить изменения
- Готовая дельта от WB sync

### 6. Обновление index_cabinet() ✅

**Файл:** `gpt_integration/ai_chat/RAG/indexer.py:716`

**Изменения:**
```python
async def index_cabinet(
    self,
    cabinet_id: int,
    full_rebuild: bool = False,
    changed_ids: Optional[Dict[str, List[int]]] = None
) -> Dict[str, Any]:
```

**Поддерживает два режима:**
1. **Event-driven** (changed_ids): Извлечение только измененных данных
2. **Full rebuild**: Полная переиндексация (weekly)

**Обновление timestamps:**
- Event-driven: обновляет `last_incremental_at`
- Full rebuild: обновляет `last_indexed_at` + `last_incremental_at`

### 7. API Endpoint ✅

**Файл:** `gpt_integration/ai_chat/RAG/api.py:53`

**Обновлено:**
```python
@router.post("/index/{cabinet_id}")
async def trigger_indexing(
    cabinet_id: int,
    full_rebuild: bool = False,
    request_body: Optional[IndexRequest] = None,
    _: None = Depends(_verify_api_key)
):
```

**Новые параметры:**
- `full_rebuild` (query param): Тип индексации
- `request_body.changed_ids` (JSON body): Дельта от WB sync

**Возвращает:**
```json
{
  "status": "success",
  "cabinet_id": 1,
  "indexing_mode": "incremental",
  "total_chunks": 2350,
  "metrics": {
    "new_chunks": 45,
    "updated_chunks": 23,
    "skipped_chunks": 1920,
    "embeddings_generated": 68
  }
}
```

---

## 🔄 Что осталось (Этап 2: 30%)

### Этап 2.8: Обновить Celery tasks ⏳

**Файл:** `server/app/features/rag/tasks.py` (или где находится)

**Нужно:**
1. Обновить сигнатуру `index_rag_for_cabinet`:
   ```python
   @celery_app.task
   def index_rag_for_cabinet(
       cabinet_id: int,
       full_rebuild: bool = False,
       changed_ids: Optional[Dict[str, List[int]]] = None
   ):
   ```

2. Добавить task для weekly full rebuild:
   ```python
   @celery_app.task
   def full_rebuild_all_cabinets_rag():
       """Weekly full rebuild для всех кабинетов."""
       # ... implementation ...
   ```

3. Обновить Celery Beat schedule:
   ```python
   # УДАЛИТЬ: Инкрементальную индексацию по расписанию
   # ОСТАВИТЬ: Только weekly full rebuild
   "index-full-rebuild-rag": {
       "task": "app.features.rag.tasks.full_rebuild_all_cabinets_rag",
       "schedule": crontab(hour=3, minute=0, day_of_week=0),
   }
   ```

### Этап 2.9: Модифицировать WB sync task ⏳

**Файл:** `server/app/features/sync/tasks.py`

**Нужно:**
1. Собирать дельту изменений во время sync:
   ```python
   changed_ids = {
       "orders": [id for id in new_or_updated_orders],
       "products": [id for id in new_or_updated_products],
       "stocks": [id for id in new_or_updated_stocks],
       "reviews": [id for id in new_or_updated_reviews],
       "sales": [id for id in new_or_updated_sales]
   }
   ```

2. Триггер RAG индексации после успешного sync:
   ```python
   if changed_ids:
       index_rag_for_cabinet.delay(cabinet_id, changed_ids=changed_ids)
   ```

---

## 📊 Оставшиеся этапы

### Этап 3: Полная переиндексация с очисткой ⏳

**Задачи:**
- Реализовать `identify_stale_chunks()` - поиск устаревших чанков
- Реализовать `cleanup_stale_chunks()` - удаление устаревших
- Интегрировать в `full_rebuild` режим

**Цель:** Еженедельная очистка устаревших данных

### Этап 4: Мониторинг и метрики ⏳

**Задачи:**
- Добавить детальное логирование метрик
- Endpoint `/v1/rag/metrics/{cabinet_id}` для статистики
- Dashboard (опционально)

### Этап 5: Тестирование ⏳

**Задачи:**
- Unit тесты для новых методов
- Integration тесты для Event-driven flow
- E2E тесты для полной переиндексации
- Нагрузочное тестирование

---

## 🎯 Следующие шаги

### Приоритет 1: Завершить Этап 2
1. ✅ Обновить Celery tasks (2.8)
2. ✅ Модифицировать WB sync task (2.9)

### Приоритет 2: Запустить миграции
```bash
# 1. Миграция основной БД (20 индексов)
cd server
alembic upgrade head

# 2. Миграция RAG БД (chunk_hash)
psql $RAG_DATABASE_URL < gpt_integration/ai_chat/RAG/migrations/001_add_chunk_hash.sql

# 3. Заполнение chunk_hash для существующих записей
python -m gpt_integration.ai_chat.RAG.migrations.001_populate_chunk_hash
```

### Приоритет 3: Тестирование
1. Протестировать Event-driven flow на тестовом кабинете
2. Проверить метрики и логи
3. Убедиться, что RAG обновляется после WB sync

---

## 📁 Измененные файлы

### Документация
- ✅ `docs/RAG/INCREMENTAL_INDEXING_PLAN.md` - обновлена архитектура
- ✅ `docs/RAG/STAGE1_API_SPECIFICATION.md` - обновлен API spec
- ✅ `docs/RAG/EVENT_DRIVEN_ARCHITECTURE.md` - **НОВЫЙ**

### Код
- ✅ `gpt_integration/ai_chat/RAG/models.py` - добавлен chunk_hash
- ✅ `gpt_integration/ai_chat/RAG/indexer.py`:
  - `calculate_chunk_hash()` метод
  - `extract_data_by_ids()` метод
  - `index_cabinet()` обновлен для Event-driven
- ✅ `gpt_integration/ai_chat/RAG/api.py` - обновлен endpoint

### Миграции
- ✅ `server/app/alembic/versions/009_add_timestamp_indexes_for_rag.py`
- ✅ `gpt_integration/ai_chat/RAG/migrations/001_add_chunk_hash.sql`
- ✅ `gpt_integration/ai_chat/RAG/migrations/001_populate_chunk_hash.py`

### TODO (осталось)
- ⏳ `server/app/features/rag/tasks.py` - обновить Celery tasks
- ⏳ `server/app/features/sync/tasks.py` - добавить RAG trigger
- ⏳ `server/app/core/celery_app.py` - обновить Beat schedule

---

## 🔍 Ключевые отличия Event-driven от Scheduled

### ДО (Scheduled):
```
Celery Beat (каждые 6 часов)
  ↓
index_all_cabinets_rag()
  ↓
Запрос БД: SELECT * WHERE updated_at > last_incremental_at
  ↓
RAG индексация
```

**Проблемы:**
- Задержка до 6 часов
- Нужно запрашивать БД по timestamp
- Индексация даже если sync сбоит

### ПОСЛЕ (Event-driven):
```
WB sync успешен
  ↓
Готовая дельта: {orders: [12345], products: [98765]}
  ↓
index_rag_for_cabinet.delay(cabinet_id, changed_ids=delta)
  ↓
Запрос БД: SELECT * WHERE id IN (12345, 98765)  -- Indexed lookup!
  ↓
RAG индексация
```

**Преимущества:**
- Near real-time (сразу после sync)
- Готовая дельта (не нужно искать по timestamp)
- Нет индексации если sync сбоит

---

## 📈 Ожидаемые результаты

### Производительность
- **До:** 90 секунд на полную индексацию (~1500 чанков)
- **После:** 5-10 секунд на инкрементальную (~150 чанков)
- **Ускорение:** 9-18x

### Экономия API
- **До:** 1500 embeddings каждые 6 часов = $1.8/месяц на кабинет
- **После:** ~150 embeddings каждые 15 минут (только изменения) = ~$0.18/месяц
- **Экономия:** 90%

### Актуальность
- **До:** Задержка до 6 часов
- **После:** Near real-time (сразу после WB sync)
- **Улучшение:** Мгновенная актуальность

---

## ⚠️ Важные замечания

### 1. WB sync должен собирать дельту
Sync task должен отслеживать какие записи были добавлены/обновлены и передавать ID в RAG индексацию.

### 2. Fallback на full rebuild
Если changed_ids не передан (или пустой), RAG индексация пропускается. Weekly full rebuild гарантирует консистентность.

### 3. Миграции обязательны
Без миграций (chunk_hash + индексы) система не будет работать оптимально.

### 4. Обратная совместимость
API endpoint сохраняет обратную совместимость:
- Без параметров: делает full rebuild (как раньше)
- С changed_ids: делает Event-driven (новое)

---

**Прогресс:** 70% Этапа 2 завершено
**Следующее:** Celery tasks + WB sync trigger
**ETA:** 1-2 дня до завершения Этапа 2
