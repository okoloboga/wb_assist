# ⚙️ Документация: Система скрейпинга конкурентов (SCOMP)

## 🎯 Обзор

Этот документ описывает архитектуру, механику и алгоритмы, используемые в системе скрейпинга данных о товарах конкурентов с сайта Wildberries.

---

## 🌊 Принцип работы и флоу

Система работает по асинхронной модели с использованием фоновых задач, что позволяет не блокировать основной интерфейс бота и обрабатывать данные в фоновом режиме.

### **1. Инициация скрейпинга (через Telegram-бот)**

1.  **Пользователь** отправляет боту ссылку на страницу бренда или продавца на Wildberries (например, `https://www.wildberries.ru/brands/some-brand`).
2.  **Бот** вызывает API-эндпоинт сервера: `POST /api/v1/bot/competitors/add`.

### **2. Постановка задачи в очередь (Backend)**

1.  **API-эндпоинт** `/competitors/add` получает URL.
2.  Происходит **валидация**: проверяется формат ссылки и лимиты на количество конкурентов для данного кабинета.
3.  В таблице `competitor_links` создается новая запись с начальным статусом `pending`.
4.  Сразу после создания записи, в очередь Celery (в `scraping_queue`) отправляется новая задача `scrape_competitor_task` с `id` созданной записи.

### **3. Выполнение задачи (Celery Worker)**

1.  **Celery Worker** подхватывает задачу из очереди.
2.  Задача выполняется в изолированном Docker-контейнере.
3.  Статус конкурента в `competitor_links` обновляется на `scraping`.
4.  Создается и настраивается экземпляр браузера **Selenium Chrome**.

### **4. Сбор данных (Scraper)**

1.  **Основная функция `scrape_competitor`** начинает работу.
2.  **Получение ссылок на товары:**
    -   Вызывается функция `get_product_urls`, которая открывает основную страницу конкурента.
    -   Скрипт динамически **прокручивает страницу вниз** несколько раз, чтобы подгрузить все товары (lazy loading).
    -   Собираются ссылки на страницы отдельных товаров (до лимита, указанного в `COMPETITOR_MAX_PRODUCTS_PER_LINK`).
3.  **Сбор данных о каждом товаре:**
    -   Скрейпер последовательно переходит по каждой собранной ссылке.
    -   Вызывается функция `scrape_product_data` для каждой страницы товара.
    -   С помощью **CSS-селекторов и XPath** извлекаются данные: название, цена (текущая и оригинальная), бренд, категория, рейтинг и описание.
    -   Для получения полного описания симулируется **клик по кнопке "Развернуть описание"**.

### **5. Сохранение в базу данных**

1.  После завершения скрейпинга, задача `scrape_competitor_task` обрабатывает результаты.
2.  **Если скрейпинг прошел успешно:**
    -   Вызывается `CompetitorProductCRUD.delete_by_competitor`, чтобы **удалить все старые товары** этого конкурента. Это обеспечивает актуальность данных при повторных скрейпингах.
    -   Новые данные о товарах сохраняются в таблицу `competitor_products`.
    -   Основная запись в `competitor_links` обновляется: устанавливается `status='completed'`, сохраняется извлеченное имя конкурента (`competitor_name`) и общее количество найденных товаров (`products_count`).
3.  **Если скрейпинг не удался** (например, не найдено ни одного товара):
    -   Статус конкурента в `competitor_links` обновляется на `error`.
    -   В поле `error_message` записывается причина ошибки.

---

## 🔧 Технические механики и алгоритмы

### **Настройка браузера (`get_driver`)**

Для обхода систем защиты от ботов и для корректной работы в Docker-контейнере используется ряд техник:

-   **Системный `chromedriver`:** Вместо `webdriver-manager` (который может скачивать несовместимые версии), используется `chromedriver`, установленный в Docker-контейнере вместе с браузером `chromium`. Это гарантирует совместимость версий.
-   **Виртуальный дисплей:** Скрейпинг запускается в режиме **`headless=False`** (с видимым окном), чтобы имитировать поведение реального пользователя. Чтобы это работало в серверной среде без графического интерфейса, используется библиотека `pyvirtualdisplay`, которая создает виртуальный экран (`Xvfb`).
-   **Маскировка автоматизации:** Применяются специальные опции Chrome и CDP-команды (Chrome DevTools Protocol) для скрытия признаков того, что браузер управляется Selenium (например, удаляется переменная `navigator.webdriver`).
-   **Оптимизация:** Для ускорения загрузки страниц отключена загрузка изображений.

### **Взаимодействие с базой данных (`crud.py`)**

-   **`CompetitorLinkCRUD`**: Управляет основной записью о конкуренте (создание, обновление статуса, удаление).
-   **`CompetitorProductCRUD`**: Управляет записями о товарах конкурента (создание, обновление, удаление, получение списка категорий).
-   **Каскадное удаление:** Модели настроены так, что при удалении записи `CompetitorLink` автоматически удаляются все связанные с ней товары из `competitor_products`. Аналогично, при удалении `WBCabinet` удаляются все связанные с ним конкуренты.

### **API эндпоинты (`routes.py`)**

Система предоставляет несколько ключевых эндпоинтов для взаимодействия с ботом:

-   `POST /competitors/add`: Добавить нового конкурента и запустить скрейпинг.
-   `GET /competitors`: Получить список всех добавленных конкурентов (независимо от статуса).
-   `GET /competitors/{id}/products`: Получить список товаров конкретного конкурента.
-   `GET /competitors/products/{id}`: Получить детальную информацию о конкретном товаре.
-   `DELETE /competitors/{id}`: Удалить конкурента и все связанные с ним данные.

---

## 🗄️ Структура данных в БД

-   **`competitor_links`**: Основная таблица, хранит URL конкурента, его имя, статус (`pending`, `scraping`, `completed`, `error`), количество товаров и время последнего скрейпинга. Связана с `wb_cabinets`.
-   **`competitor_products`**: Таблица с детальной информацией о каждом товаре конкурента. Связана с `competitor_links`.

Эта архитектура позволяет гибко управлять процессом скрейпинга, отслеживать его статус и хранить полученные данные в структурированном виде для дальнейшего анализа и отображения в боте.
