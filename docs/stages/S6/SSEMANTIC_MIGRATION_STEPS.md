## Этап 0. Аналитика и подготовка

- **Цель:** зафиксировать текущее состояние и спланировать минимально больной перенос.
- **Действия:**
  - **Изучить текущую реализацию:**
    - `docs/stages/S6/SSEMANTIC.md` — механика и архитектура старой функции;
    - `server/app/features/competitors/models.py` — модель `CompetitorSemanticCore`;
    - `server/app/features/competitors/routes.py` — эндпоинт `POST /competitors/{competitor_id}/semantic-core`;
    - `server/app/features/competitors/tasks.py` — задача `generate_semantic_core_task`;
    - `bot/handlers/competitors.py` — хендлеры `competitor_semantic_core_*`;
    - `bot/keyboards/keyboards.py` — кнопка «💎 Семантическое ядро» в меню конкурента.
  - **Зафиксировать требования к новой логике:**
    - точка входа — меню **AI‑помощника**, а не «Конкуренты»;
    - категория выбирается из **общего списка категорий по всем конкурентам кабинета**;
    - описания товаров берутся **по всем конкурентам** в выбранной категории;
    - результат хранится и отдается **на уровне кабинета**, а не конкретного конкурента.

Результат этапа: понятная схема «как сейчас» и финальные требования «как должно быть».

---

## Этап 1. Backend: новая модель и CRUD под агрегированное ядро

- **Цель:** ввести отдельную сущность для «кабинетного» семантического ядра, не ломая текущий функционал.

- **Шаги:**
  1. **Создать модель агрегированного ядра** (условно `SemanticCore` / `CabinetSemanticCore`) в новом модуле или рядом с конкурентами:
     - поля:  
       - `id`  
       - `cabinet_id` (FK → `wb_cabinets.id`)  
       - `category_name`  
       - `core_data`  
       - `status` (`pending / processing / completed / error`)  
       - `error_message` (опционально)  
       - `created_at`, `completed_at`  
     - ограничение уникальности: `(cabinet_id, category_name)` — одно ядро на кабинет+категорию.
  2. **Добавить CRUD‑слой**:
     - `get_or_create(cabinet_id, category_name)`;
     - `get_by_cabinet_and_category(cabinet_id, category_name)`;
     - `update_status(...)`, `save_core_data(...)`.
  3. **Подготовить Alembic‑миграцию**:
     - создать таблицу агрегированных семантических ядер;
     - не трогать существующую `competitor_semantic_cores` (пока).

Результат этапа: в БД и коде сервера появилось место, куда можно сохранять ядро «по кабинету и категории».

---

## Этап 2. Backend: новые эндпоинты и Celery‑задача

- **Цель:** реализовать серверную механику генерации ядра по категории **для всех конкурентов кабинета**.

- **Шаги:**
  1. **Новый эндпоинт Bot API для запуска генерации**, например:  
     - `POST /api/v1/bot/semantic-cores/generate`  
     - вход: `{"category_name": "..."}`, query‑параметры: `telegram_id`, `force=false|true`;
     - внутри:
       - по `telegram_id` получить `cabinet_id`;
       - по `(cabinet_id, category_name)` найти или создать запись агрегированного ядра;
       - если запись `completed` и `force=false` → вернуть уже сгенерированный результат;
       - иначе → сбросить статус в `pending/processing`, запустить Celery‑задачу.
  2. **Новый эндпоинт Bot API для списка ядер по кабинету**, например:  
     - `GET /api/v1/bot/semantic-cores/` → список всех `completed` ядер по `cabinet_id`.
  3. **Новый эндпоинт Bot API для деталей ядра**, например:  
     - `GET /api/v1/bot/semantic-cores/{core_id}` → `core_data` и метаданные (проверка, что ядро принадлежит кабинету пользователя).
  4. **Celery‑задача для генерации агрегированного ядра**, например `generate_cabinet_semantic_core_task`:
     - вход: `semantic_core_id` (новой сущности);
     - по `semantic_core_id` получить `cabinet_id` и `category_name`;
     - найти всех конкурентов кабинета (`CompetitorLink` по `cabinet_id`);
     - для всех конкурентов выбрать товары с нужной категорией (`CompetitorProduct.category == category_name`);
     - собрать все описания в один большой текст;
     - обрезать по лимиту `SEMANTIC_CORE_MAX_TEXT_LENGTH`;
     - отправить текст в GPT‑сервис (`/api/v1/semantic-core/generate`);
     - сохранить результат в `core_data`, статус → `completed` / `error`;
     - отправить webhook‑уведомление боту `semantic_core_ready` с `core_id` и `category_name`.

Результат этапа: backend умеет запускать и завершать генерацию ядра «по кабинету и категории» и оповещать бота.

---

## Этап 3. Bot: новая точка входа в AI‑помощнике

- **Цель:** перевести UX входа в семантическое ядро из «Конкуренты» в «AI‑помощник», не ломая старый код до завершения тестов.

- **Шаги:**
  1. **Обновить клавиатуру AI‑помощника** (`ai_assistant_keyboard` в `bot/keyboards/keyboards.py`):
     - добавить кнопку `💎 Семантическое ядро` с `callback_data="semantic_core_menu"`.
  2. **Создать новый хендлер для меню семантического ядра**, например в `bot/handlers/semantic_core.py`:
     - `@router.callback_query(F.data == "semantic_core_menu")`:
       - по `user_id` вызвать новый Bot API эндпоинт для списка доступных категорий (см. Этап 4);
       - показать список категорий как inline‑кнопки вида `semantic_core_category_{category_slug}`.
  3. **Создать хендлер запуска генерации**, например:
     - `@router.callback_query(F.data.startswith("semantic_core_category_"))`:
       - извлечь `category_name` (или slug → name);
       - вызвать `POST /api/v1/bot/semantic-cores/generate`;
       - если ответ `already_exists` → сразу показать ядро;
       - если запуск новой задачи → показать сообщение «Запустил анализ… пришлю результат, как только будет готов».
  4. **Обработчик готового результата**:
     - доработать/использовать существующий webhook‑хендлер `semantic_core_ready`:
       - получать `core_id`, `category_name`, `telegram_text`;
       - отправлять пользователю итоговое сообщение (разбивая по частям, как описано в `SSEMANTIC.md`).

Результат этапа: пользователь заходит в семантическое ядро **через AI‑помощник**, а не через карточку конкурента.

---

## Этап 4. Bot & Backend: категории по всем конкурентам

- **Цель:** научиться показывать пользователю **общий список категорий** по всем конкурентам кабинета.

- **Шаги:**
  1. **Backend: эндпоинт для категорий**, например:
     - `GET /api/v1/bot/semantic-cores/categories` с `telegram_id`;
     - логика:
       - по `telegram_id` найти `cabinet_id`;
       - выбрать из `CompetitorProduct` все уникальные `category` по всем `CompetitorLink` этого кабинета;
       - вернуть список категорий (опционально — отсортировать/ограничить по количеству товаров).
  2. **Bot: клиентский метод**, расширить `bot/api/client.py`:
     - метод `get_semantic_core_categories(user_id: int)` → вызывает новый эндпоинт;
     - используется в хендлере `semantic_core_menu` (Этап 3).

Результат этапа: кнопка «Семантическое ядро» в AI‑помощнике показывает корректный список категорий по всем конкурентам.

---

## Этап 5. Отключение старой функции у конкурентов

- **Цель:** убрать тему семантического ядра из раздела «Конкуренты», чтобы не было дубля функционала.

- **Шаги:**
  1. **Bot: убрать UI‑элементы:**
     - удалить кнопку `💎 Семантическое ядро` из меню конкурента / списка товаров (`bot/keyboards/keyboards.py`, `create_competitor_products_keyboard` и др.);
     - удалить/отключить хендлеры:
       - `@router.callback_query(F.data.startswith("competitor_semantic_core_"))`;
       - `@router.callback_query(F.data.startswith("competitor_category_core_"))`;
     - почистить неиспользуемые импорты (`create_semantic_core_categories_keyboard`, если он больше нигде не нужен).
  2. **Backend: пометить старый API как устаревший:**
     - либо временно оставить `POST /competitors/{competitor_id}/semantic-core` с пометкой «deprecated» (для плавного отката);
     - либо сразу убрать, если уверены, что ни один клиент его не вызывает.
  3. **Документация:**
     - обновить `docs/stages/S6/SSEMANTIC.md`: описать новую точку входа и то, что ядро теперь агрегировано по всем конкурентам;
     - явно указать, что функционал на уровне одного конкурента удален.

Результат этапа: пользователю доступен **только** новый сценарий через AI‑помощник; следов старой кнопки у конкурентов нет.

---

## Этап 6. Очистка старых данных и кода (по желанию)

- **Цель:** убрать из проекта неиспользуемый код и таблицы, связанные только со старой реализацией.

- **Шаги:**
  1. **База данных:**
     - решить стратегию по таблице `competitor_semantic_cores`:
       - оставить как архив (ничего не трогать);
       - либо удалить через отдельную Alembic‑миграцию после N недель стабильной работы новой схемы.
  2. **Код:**
     - удалить старые CRUD‑классы и модели, если они используются только для старого ядра;
     - удалить старые Celery‑задачи, которые больше не вызываются;
     - проверить импорты и «мертвый» код.
  3. **Документация:**
     - синхронизировать `SSEMANTIC_MIGRATION.md` и этот файл (этапы);
     - добавить пометку о том, что старая реализация выведена из эксплуатации.

Результат этапа: кодовая база и БД очищены от технического долга, связанным с прежней реализацией ядра по одному конкуренту.

---

## Этап 7. Тестирование и релиз

- **Цель:** убедиться, что перенос прошёл без регрессий и новый сценарий понятен пользователям.

- **Шаги:**
  1. **Юнит‑ и интеграционные тесты:**
     - тест выбора категорий по всем конкурентам;
     - тест запуска Celery‑задачи;
     - тест обработки ошибок (нет товаров в категории, GPT вернул ошибку и т.п.).
  2. **E2E‑сценарий через Telegram:**
     - зайти в меню 🧠 AI‑помощника;
     - нажать «💎 Семантическое ядро»;
     - выбрать категорию;
     - дождаться и проверить корректность результата (структура и разбиение на сообщения).
  3. **Нагрузочное/прагматичное тестирование:**
     - проверить категории, где много конкурентов и товаров;
     - оценить, достаточно ли лимита `SEMANTIC_CORE_MAX_TEXT_LENGTH`.
  4. **Релиз:**
     - выкатить на staging, затем на production;
     - при необходимости — коротко описать изменения в changelog/релиз‑нотах.

Итог: функция «Семантическое ядро» живёт в AI‑помощнике, работает по всем конкурентам сразу, а старая реализация в разделе «Конкуренты» упразднена.


