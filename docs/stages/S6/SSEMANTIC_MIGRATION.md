# 🔄 Перенос функции: Семантическое ядро

## 🎯 Цель миграции

Перенести функцию **"Семантическое ядро"** из раздела **"Конкуренты"** в раздел **"AI-помощник"** с изменением логики работы:

- **Было:** Семантическое ядро собирается по категории товаров **одного конкретного конкурента**
- **Стало:** Семантическое ядро собирается по категории товаров **ВСЕХ конкурентов** пользователя

---

## 📋 Текущее состояние

### Расположение функции

**Раздел:** ⚖️ Конкуренты  
**Подраздел:** Меню конкретного конкурента  
**Кнопка:** `💎 Семантическое ядро`

### Текущая логика работы

1. Пользователь заходит в меню конкурента
2. Нажимает кнопку "💎 Семантическое ядро"
3. Выбирает категорию из списка категорий **этого конкурента**
4. Система собирает описания товаров **только этого конкурента** в выбранной категории
5. Генерируется семантическое ядро на основе описаний одного конкурента

### Текущая структура данных

**Таблица БД:** `competitor_semantic_cores`
- `id` - ID записи
- `competitor_link_id` - ID конкретного конкурента (ForeignKey → `competitor_links.id`)
- `category_name` - Название категории
- `core_data` - Сгенерированное семантическое ядро
- `status` - Статус генерации (pending, processing, completed, error)
- `created_at` - Дата создания
- `completed_at` - Дата завершения

**Ограничение:** Одна запись = один конкурент + одна категория

---

## 🎯 Целевое состояние

### Новое расположение функции

**Раздел:** 🤖 AI-помощник  
**Новая функция:** `💎 Семантическое ядро`

### Новая логика работы

1. Пользователь заходит в меню "🤖 AI-помощник"
2. Нажимает кнопку "💎 Семантическое ядро"
3. Выбирает категорию из списка **всех уникальных категорий всех конкурентов** пользователя
4. Система собирает описания товаров **ВСЕХ конкурентов** пользователя в выбранной категории
5. Генерируется семантическое ядро на основе описаний всех конкурентов

### Новая структура данных

**Новая таблица БД:** `semantic_cores` (или `cabinet_semantic_cores`)
- `id` - ID записи
- `cabinet_id` - ID кабинета WB пользователя (ForeignKey → `wb_cabinets.id`)
- `category_name` - Название категории
- `core_data` - Сгенерированное семантическое ядро
- `status` - Статус генерации (pending, processing, completed, error)
- `created_at` - Дата создания
- `completed_at` - Дата завершения

**Ограничение:** Одна запись = один кабинет + одна категория (объединяет данные всех конкурентов)

---

## 🔧 Технические изменения

### 1. База данных

#### Создание новой таблицы

```python
class SemanticCore(Base):
    """Модель семантического ядра по категории для всех конкурентов кабинета"""
    __tablename__ = "semantic_cores"

    id = Column(Integer, primary_key=True, index=True)
    cabinet_id = Column(Integer, ForeignKey("wb_cabinets.id", ondelete="CASCADE"), nullable=False, index=True)
    category_name = Column(String(255), nullable=False)
    core_data = Column(Text, nullable=True)
    status = Column(String(20), nullable=False, default='pending')  # pending, processing, completed, error
    error_message = Column(Text, nullable=True)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    completed_at = Column(DateTime(timezone=True), nullable=True)

    # Связи
    cabinet = relationship("WBCabinet", back_populates="semantic_cores")

    # Индексы и ограничения
    __table_args__ = (
        UniqueConstraint('cabinet_id', 'category_name', name='uq_cabinet_category_semantic_core'),
        Index('idx_semantic_core_cabinet_category', 'cabinet_id', 'category_name'),
    )
```

#### Миграция данных (опционально)

Если есть существующие данные в `competitor_semantic_cores`, можно их объединить или оставить как есть. Рекомендуется оставить старую таблицу на период миграции и затем удалить.

#### Удаление старой таблицы

После завершения миграции и проверки работоспособности новой функциональности:
- Удалить таблицу `competitor_semantic_cores`
- Удалить связанные CRUD-операции
- Удалить старые API-эндпоинты

---

### 2. Backend (server)

#### Новые/Измененные файлы

**`server/app/features/semantic_core/`** (новый модуль)
```
semantic_core/
├── __init__.py
├── models.py              # Новая модель SemanticCore
├── schemas.py             # Pydantic схемы для API
├── crud.py                # CRUD операции
├── router.py              # FastAPI эндпоинты
└── tasks.py               # Celery задачи
```

#### Новые API эндпоинты

**`GET /api/v1/bot/semantic-cores/categories`**
- Получить список всех уникальных категорий из всех конкурентов кабинета пользователя
- Query параметры: `telegram_id`

**`POST /api/v1/bot/semantic-cores/generate`**
- Запустить генерацию семантического ядра по категории для всех конкурентов
- Body: `{"category_name": "..."}`
- Query параметры: `telegram_id`, `force` (bool, опционально)

**`GET /api/v1/bot/semantic-cores/{core_id}`**
- Получить детальную информацию о семантическом ядре
- Query параметры: `telegram_id`

**`GET /api/v1/bot/semantic-cores/`**
- Получить список всех сгенерированных семантических ядер кабинета
- Query параметры: `telegram_id`

#### Измененная Celery-задача

**`generate_semantic_core_task`** (переименовать в `generate_cabinet_semantic_core_task`)

**Логика:**
1. Получает `cabinet_id` и `category_name`
2. Находит **всех конкурентов** кабинета
3. Для каждого конкурента собирает описания товаров в указанной категории
4. Объединяет все описания в один текстовый блок
5. Обрезает текст по лимиту `SEMANTIC_CORE_MAX_TEXT_LENGTH`
6. Отправляет запрос в GPT-сервис
7. Сохраняет результат в новую таблицу `semantic_cores`
8. Отправляет webhook-уведомление боту

**Код сбора описаний:**
```python
# Найти всех конкурентов кабинета
competitors = db.query(CompetitorLink).filter(
    CompetitorLink.cabinet_id == cabinet_id
).all()

# Собрать описания всех товаров всех конкурентов по категории
all_descriptions = []
for competitor in competitors:
    products = db.query(CompetitorProduct).filter(
        CompetitorProduct.competitor_link_id == competitor.id,
        CompetitorProduct.category == category_name
    ).all()
    
    for product in products:
        if product.description:
            all_descriptions.append(product.description)

# Объединить описания
combined_text = "\n\n".join(all_descriptions)
```

#### Удаление старых эндпоинтов

- `POST /api/v1/bot/competitors/{competitor_id}/semantic-core` → удалить
- `GET /api/v1/bot/competitors/{competitor_id}/categories` → оставить (нужен для других функций)

---

### 3. Bot

#### Изменения в клавиатурах

**`bot/keyboards/keyboards.py`**

**Удалить из `create_competitor_menu_keyboard`:**
```python
[InlineKeyboardButton(text="💎 Семантическое ядро", callback_data=f"competitor_semantic_core_{competitor_id}")]
```

**Добавить в `ai_assistant_keyboard`:**
```python
[InlineKeyboardButton(text="💎 Семантическое ядро", callback_data="semantic_core_menu")]
```

#### Новые обработчики

**`bot/handlers/semantic_core.py`** (новый файл)

```python
@router.callback_query(F.data == "semantic_core_menu")
async def show_semantic_core_categories(callback: CallbackQuery):
    """Показать список категорий для выбора семантического ядра"""
    # Запросить список всех категорий всех конкурентов через API
    # Отобразить их в виде inline-кнопок

@router.callback_query(F.data.startswith("semantic_core_category_"))
async def start_semantic_core_generation(callback: CallbackQuery):
    """Запустить генерацию семантического ядра для выбранной категории"""
    # Извлечь category_name из callback_data
    # Вызвать API для запуска генерации
    # Показать сообщение о начале процесса

# Обработчик webhook-уведомления о готовности ядра
# (уже есть в bot/handlers/webhook.py, нужно обновить)
```

#### Удаление старых обработчиков

**`bot/handlers/competitors.py`**
- Удалить `show_semantic_core_categories` (callback: `competitor_semantic_core_`)
- Удалить `start_semantic_core_generation` (callback: `competitor_category_core_`)
- Удалить клавиатуру `create_semantic_core_categories_keyboard` из импортов (если используется только здесь)

#### Обновление API-клиента

**`bot/api/client.py`**

**Добавить новые методы:**
```python
async def get_semantic_core_categories(self, user_id: int) -> BotAPIResponse:
    """Получить список всех категорий всех конкурентов кабинета"""
    
async def generate_semantic_core(self, category_name: str, user_id: int, force: bool = False) -> BotAPIResponse:
    """Запустить генерацию семантического ядра по категории для всех конкурентов"""
    
async def get_semantic_core(self, core_id: int, user_id: int) -> BotAPIResponse:
    """Получить детальную информацию о семантическом ядре"""
```

**Удалить старые методы:**
- `generate_semantic_core` (старый, который принимал `competitor_id`)

#### Обновление webhook-обработчика

**`bot/handlers/webhook.py`**

Обновить обработчик `semantic_core_ready` для работы с новой структурой данных (без привязки к конкретному конкуренту).

---

### 4. GPT-сервис (gpt_integration)

**Изменений не требуется** - эндпоинт `POST /api/v1/semantic-core/generate` остается без изменений, так как он принимает только текст и возвращает результат.

---

## 📝 Промпт для ChatGPT

**Изменений не требуется** - промпт остается прежним (см. `docs/stages/S6/SSEMANTIC.md`, раздел "🤖 Промпт для ChatGPT").

Единственное изменение в тексте промпта (опционально): можно добавить упоминание, что анализируются описания нескольких конкурентов для более полного охвата семантики.

---

## 🔄 Порядок выполнения миграции

### Этап 1: Подготовка (Backend)

1. ✅ Создать новую модель `SemanticCore` в БД
2. ✅ Создать миграцию Alembic для новой таблицы
3. ✅ Создать новый модуль `server/app/features/semantic_core/`
4. ✅ Реализовать CRUD-операции для новой таблицы
5. ✅ Создать новые API-эндпоинты
6. ✅ Реализовать новую Celery-задачу (сбор данных всех конкурентов)

### Этап 2: Интеграция (Bot)

7. ✅ Обновить клавиатуры (убрать из конкурентов, добавить в AI-помощник)
8. ✅ Создать новый обработчик `bot/handlers/semantic_core.py`
9. ✅ Обновить API-клиент в боте
10. ✅ Обновить webhook-обработчик
11. ✅ Удалить старые обработчики из `competitors.py`

### Этап 3: Тестирование

12. ✅ Протестировать сбор категорий всех конкурентов
13. ✅ Протестировать генерацию ядра для категории
14. ✅ Проверить корректность объединения данных всех конкурентов
15. ✅ Протестировать отображение результатов в боте

### Этап 4: Очистка (после успешного тестирования)

16. ✅ Удалить старую таблицу `competitor_semantic_cores` (или оставить для истории)
17. ✅ Удалить старые эндпоинты
18. ✅ Удалить старые CRUD-операции
19. ✅ Удалить старые Celery-задачи
20. ✅ Обновить документацию

---

## ⚠️ Важные замечания

### Обратная совместимость

- Старая функция должна быть **полностью удалена** из раздела "Конкуренты"
- Если есть существующие данные в `competitor_semantic_cores`, их можно:
  - Оставить без изменений (для истории)
  - Удалить (если они больше не нужны)
  - Мигрировать в новую таблицу (если требуется сохранить доступ к старым ядрам)

### Производительность

- При сборе данных всех конкурентов может быть **больше данных**, чем для одного конкурента
- Нужно убедиться, что лимит `SEMANTIC_CORE_MAX_TEXT_LENGTH` достаточен
- Возможно, потребуется оптимизация запросов к БД при сборе описаний

### UX изменения

- Пользователи больше не смогут генерировать ядро для одного конкретного конкурента
- Теперь ядро будет агрегированным по всем конкурентам - это нужно четко объяснить пользователю
- В интерфейсе можно добавить подсказку: *"Ядро собирается по всем вашим конкурентам для более полного охвата семантики"*

---

## 📚 Связанная документация

- **Механика работы:** `docs/stages/S6/SSEMANTIC.md`
- **Система конкурентов:** `docs/stages/S6/SCOMP.md`
- **AI-помощник:** `gpt_integration/ai_chat/docs/UPDATES.md`

---

## ✅ Checklist миграции

### Backend
- [ ] Создана модель `SemanticCore`
- [ ] Создана миграция БД
- [ ] Создан модуль `semantic_core/`
- [ ] Реализованы CRUD-операции
- [ ] Созданы новые API-эндпоинты
- [ ] Реализована новая Celery-задача
- [ ] Обновлены webhook-уведомления

### Bot
- [ ] Удалена кнопка из меню конкурента
- [ ] Добавлена кнопка в меню AI-помощника
- [ ] Создан новый обработчик `semantic_core.py`
- [ ] Обновлен API-клиент
- [ ] Обновлен webhook-обработчик
- [ ] Удалены старые обработчики

### Тестирование
- [ ] Тест получения категорий всех конкурентов
- [ ] Тест генерации ядра
- [ ] Тест объединения данных
- [ ] Тест отображения в боте

### Очистка
- [ ] Удалена старая таблица (или оставлена)
- [ ] Удалены старые эндпоинты
- [ ] Удалены старые задачи
- [ ] Обновлена документация

---

**Дата создания документа:** 2025-01-XX  
**Статус:** 🔄 В работе






