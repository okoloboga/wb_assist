# 📊 Реализация динамической аналитики с графиками

## ✅ Что реализовано

### 1. Серверный эндпоинт `/api/v1/bot/analytics/daily-trends`

**Файл:** `server/app/features/bot_api/service.py` → `get_daily_trends()`

**Что делает:**
- Получает данные за N дней (по умолчанию 10, настраивается через `ANALYTICS_DAYS_WINDOW`)
- Считает по дням: заказы, отмены (из `wb_orders`), выкупы и возвраты (из `wb_sales`)
- Вычисляет среднюю оценку отзывов за каждый день (из `wb_reviews`)
- Формирует агрегаты: общее количество событий, buyout_rate, return_rate
- Строит топ-товары (использует `WBProduct.name`, фолбэк на `nm_id`)
- Генерирует график matplotlib (4 линии: orders/cancellations/buyouts/returns)
- Возвращает base64-кодированное изображение графика

**Схемы данных:**
- `DailyTrendPoint`: данные за один день
- `DailyTrendsAggregates`: итоговые показатели
- `DailyTrendsTopProduct`: топ-товар
- `DailyTrendsData`: полная структура ответа с графиком

### 2. Интеграция в GPT-сервис

**Файл:** `gpt_integration/analysis/service.py`

**Изменения:**
- Добавлена функция `_fetch_daily_trends()` для запроса нового эндпоинта
- Заменён источник данных с `_fetch_analytics_sales` на `_fetch_daily_trends`
- Добавлена функция `_send_photo_to_bot()` для отправки графика через Telegram API
- График отправляется **перед** текстовыми сообщениями

**Файл:** `gpt_integration/analysis/aggregator.py`

- Добавлен ключ `daily_trends` в список обрабатываемых данных

### 3. Обновление шаблона анализа

**Файл:** `gpt_integration/analysis/LLM_ANALYSIS_TEMPLATE.md`

**Изменения:**
- Секция `TASKS`: теперь инструкция анализировать `daily_trends` вместо `sales`
- Добавлены метрики: buyout_rate, return_rate, avg_rating
- Обновлена схема JSON: новая структура `key_metrics` с totals/rates
- Обновлён формат Telegram-сообщения: акцент на динамику событий
- Добавлен запрет на разделители разрядов в числах (40_000.0 → 40000.0)

### 4. Исправление парсинга JSON

**Файл:** `gpt_integration/analysis/pipeline.py`

**Изменения:**
- Добавлен санитайзер для удаления подчёркиваний в числах перед парсингом
- Усилена логика извлечения «чистого JSON» (без markdown-блоков)
- Сохранены fallback-стратегии для совместимости

### 5. Docker Compose

**Файл:** `docker-compose.yml`

**Добавлены переменные окружения для GPT-сервиса:**
- `BOT_TOKEN` — для отправки графиков через Telegram API
- `ANALYTICS_DAYS_WINDOW` — настройка периода анализа (по умолчанию 10)

### 6. Зависимости

**Файл:** `server/requirements.txt`

- Добавлен `matplotlib>=3.7.0` для генерации графиков

## 🔄 Как работает новый флоу

1. Пользователь нажимает "🧠 AI-анализ" в боте
2. GPT-сервис запрашивает `/api/v1/bot/analytics/daily-trends`
3. Сервер:
   - Извлекает данные из БД (`wb_orders`, `wb_sales`, `wb_reviews`, `wb_products`)
   - Считает события по дням
   - Вычисляет агрегаты и топ-товары
   - Генерирует график matplotlib
   - Кодирует график в base64
   - Возвращает JSON с данными + график
4. GPT-сервис:
   - Агрегирует данные через `aggregator.py`
   - Отправляет в OpenAI с обновлённым промптом
   - Получает JSON с анализом динамики
   - **Сначала отправляет график** через Telegram API
   - Затем отправляет текстовые чанки через webhook

## 📊 Формат нового отчёта

```
📊 ДИНАМИКА ЗА 10 ДНЕЙ

Заказы: 450 шт.
Отмены: 23 шт. (5.1%)
Выкупы: 389 шт. (86.4% от заказов)
Возвраты: 12 шт. (3.1% от выкупов)

📈 ПОКАЗАТЕЛИ

• Коэффициент выкупа: 86.4%
• Коэффициент возврата: 3.1%
• Средняя оценка: 4.6

🛍️ ТОП ТОВАРОВ

1. Платье с длинным рукавом нарядное мини — 120 заказов, 104 выкупа, рейтинг 4.7
2. Пиджак приталенный с поясом — 65 заказов, 58 выкупов, рейтинг 4.6
3. Платье шелковое — 49 заказов, 43 выкупа, рейтинг 4.4

💡 ИНСАЙТЫ

• Стабильный тренд заказов последние 3 дня
• Низкий процент возвратов — качество товаров на высоте
• Пик заказов был 3 дня назад

🔍 РЕКОМЕНДАЦИИ

• Проанализировать причины отмен (5.1%)
• Поддержать топ-товары рекламой
• Пополнить остатки популярных позиций
```

## 🎨 График

График содержит 4 линии разного цвета:
- Синяя: Заказы
- Красная: Отмены
- Зелёная: Выкупы
- Оранжевая: Возвраты

Отображает динамику за период, отправляется пользователю как изображение.

## 🔧 Настройки

### Переменные окружения (.env)

```bash
# Период анализа (дни)
ANALYTICS_DAYS_WINDOW=10

# Telegram Bot Token (для отправки графиков)
BOT_TOKEN=123456:ABC-DEF...
```

### Серверный эндпоинт

```http
GET /api/v1/bot/analytics/daily-trends?telegram_id=123&days=10
Headers: {"X-API-SECRET-KEY": "..."}
```

**Query параметры:**
- `telegram_id` (обязательный) — ID пользователя
- `days` (опциональный) — количество дней, если не указано берётся из env

## ✅ Критерии приёмки (из ТЗ)

- [x] Эндпоинт возвращает динамику за N дней (настраивается)
- [x] Все 4 категории событий присутствуют: заказы/отмены/выкупы/возвраты
- [x] Вычисляются buyout_rate и return_rate
- [x] Средняя оценка отзывов включена
- [x] Топ-товары используют `WBProduct.name` с фолбэком `nm_id`
- [x] График генерируется matplotlib и отправляется пользователю
- [x] LLM-шаблон обновлён под новую структуру данных
- [x] Старый эндпоинт `/analytics/sales` не удалён (работает параллельно)

## 🐛 Известные проблемы и решения

### Проблема: JSON parsing failed (подчёркивания в числах)

**Причина:** OpenAI иногда возвращает числа в Python-формате (40_000.0)

**Решение:** Добавлен санитайзер в `pipeline.py`, который удаляет подчёркивания перед парсингом

### Проблема: График не отправляется

**Причина:** Отсутствует `BOT_TOKEN` в переменных окружения GPT-сервиса

**Решение:** Добавлен `BOT_TOKEN` в `docker-compose.yml`

## 📝 Файлы изменений

### Изменённые файлы:
- `server/app/features/bot_api/service.py` — реализация `get_daily_trends()`
- `server/app/features/bot_api/schemas.py` — новые Pydantic схемы
- `server/app/features/bot_api/routes.py` — новый роут
- `server/requirements.txt` — добавлен matplotlib
- `gpt_integration/analysis/service.py` — отправка графика + новый fetch
- `gpt_integration/analysis/aggregator.py` — поддержка `daily_trends`
- `gpt_integration/analysis/pipeline.py` — санитайзер JSON
- `gpt_integration/analysis/LLM_ANALYSIS_TEMPLATE.md` — обновлённый промпт
- `docker-compose.yml` — новые env-переменные

### Новые файлы:
- `docs/stages/S5/ANALYST.md` — техническое задание
- `docs/stages/S5/SCRAP.md` — этот файл (описание реализации)

## 🚀 Следующие шаги

1. Протестировать новый флоу в Docker
2. Проверить отправку графика пользователю
3. Убедиться, что топ-товары показывают правильные имена
4. При необходимости откорректировать промпт для более точных инсайтов

---

**Дата реализации:** 2025-11-16  
**Статус:** ✅ Реализовано, готово к тестированию
