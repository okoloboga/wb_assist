
# 📘 ТЗ: Аналитический отчёт с динамикой событий

## 🎯 Цель

Создать новый комплексный отчёт для AI-сервиса, который анализирует динамику событий Wildberries (заказы, отмены, выкупы, возвраты) за скользящий период последних N дней (по умолчанию 10). Отчёт должен формироваться на основе специализированного эндпоинта Backend-сервиса, возвращающего агрегированные метрики и график в одном ответе, и заменять текущий поток данных из `/api/v1/bot/analytics/sales` в AI-аналитике (старый эндпоинт остаётся для обратной совместимости).

## ⚙️ Переменные окружения

| Переменная | Значение по умолчанию | Назначение |
|------------|-----------------------|------------|
| `ANALYTICS_DAYS_WINDOW` | `10` | Количество дней в скользящем окне динамики. Используется и сервером, и GPT-сервисом |

> Переменная задаётся в `.env` каждого сервиса (Backend, GPT) для синхронизации периода.

## 🛠️ Backend: новый эндпоинт

- **Путь:** `GET /api/v1/bot/analytics/daily-trends`
- **Параметры запроса:**  
  - `telegram_id` *(обязательный, int)* — идентификатор пользователя в Telegram.  
  - `days` *(опционально, int)* — переопределение окна динамики; если отсутствует, используем `ANALYTICS_DAYS_WINDOW`.
- **Ответ:** `DailyTrendsAPIResponse` (новая Pydantic-схема).

### Схема ответа (предложение)
```json
{
  "status": "success",
  "analytics": {
    "meta": {
      "days_window": 10,
      "date_range": {
        "start": "2025-11-02",
        "end": "2025-11-11"
      },
      "generated_at": "2025-11-11T12:34:56+03:00"
    },
    "time_series": [
      {
        "date": "2025-11-02",
        "orders": 74,
        "cancellations": 12,
        "buyouts": 61,
        "returns": 9,
        "avg_rating": 4.62
      }
      // ...
    ],
    "aggregates": {
      "totals": {
        "orders": 812,
        "cancellations": 113,
        "buyouts": 640,
        "returns": 78
      },
      "conversion": {
        "buyout_rate_percent": 78.82,
        "return_rate_percent": 12.19
      }
    },
    "top_products": [
      {
        "nm_id": 225287280,
        "name": "Платья",
        "orders": 92,
        "cancellations": 8,
        "buyouts": 78,
        "returns": 5,
        "buyout_rate_percent": 84.78,
        "return_rate_percent": 6.41
      }
      // ...
    ],
    "chart": {
      "format": "png_base64",
      "data": "iVBORw0KGgoAAAANSUhEUgAA..."
    }
  },
  "telegram_text": "...",
  "insights": [
    {
      "type": "trend",
      "title": "Выкупы растут 3 день подряд",
      "detail": "Рост 14% относительно начала периода"
    }
  ]
}
```

### Требования к реализации

1. **Доступ к данным**
   - Заказы и отмены — таблица `wb_orders` (статус `canceled` трактуется как отмена).
   - Выкупы и возвраты — таблица `wb_sales` (`type = 'buyout'`, `type = 'return'`, `is_cancel = false`).
   - Фильтрация по `cabinet_id`, соответствующему `telegram_id` пользователя.
   - Период: последние `N` календарных дней включительно (по МСК), считается относительно начала текущего дня.

2. **Агрегаты**
   - Суммарные значения по каждой категории за период.
   - Доля выкупов: `buyouts / orders * 100` (предусмотреть защиту от деления на ноль).
   - Доля возвратов: `returns / buyouts * 100` (или от orders? требуется уточнение; пока считаем от buyouts).
   - Топ-товары (до 5 позиций): сортировка по числу заказов. Название берём из `wb_products.name`; если отсутствует — используем `str(nm_id)`.

3. **Сериализация**
   - Формат всех дат — ISO 8601.
   - Числа округлять до 2 знаков для процентов.
   - Список `time_series` должен содержать ровно `days_window` элементов (даже если нет событий — использовать нули).

4. **График**
   - Генерируется в `matplotlib` (line chart) с четырьмя линиями: `orders`, `cancellations`, `buyouts`, `returns`.
   - Цвета (рекомендация): `orders` — синий, `cancellations` — оранжевый, `buyouts` — зелёный, `returns` — красный.
   - Заголовок: `Динамика событий за {days_window} дней`. Ось X — даты (формат `DD.MM`), ось Y — количество.
   - Легенда обязательна. Включить сетку (grid) для читабельности.
   - Итоговое изображение сохраняем в буфер `BytesIO`, кодируем в base64, отдаём в поле `chart.data`.

5. **Telegram текст**
   - Возвращаемый текст должен использовать `product.name` (fallback на `nm_id`) так же, как в документации `ORDERS.md`.
   - Шаблон можно вынести в форматтер, в духе текущего `BotMessageFormatter`. В тексте включаем:
     - суммарные значения по категориям,
     - текущие buyout/return rate,
     - краткий список топ-товаров.

6. **Кэширование**
   - Использовать существующий `WBCacheManager` с TTL 5 минут (аналогично остальным бот-эндпоинтам), чтобы не перегружать БД при повторных запросах.

7. **Тесты**
   - Unit-тесты для сервиса, вычисляющего метрики (моки БД).
   - Тест проверки генерации графика (проверка на наличие Base64-строки непустой длины).
   - Тесты Pydantic-схем на корректную сериализацию.

## 🤖 GPT-сервис

1. **Новый fetcher**
   - Добавить `_fetch_daily_trends()` в `gpt_integration/service.py`.
   - В `_orchestrate_analysis` заменить обращение к `/analytics/sales` на новый эндпоинт.

2. **Агрегация**
   - В `aggregator.py` добавить секцию `daily_trends` с полями:
     - `time_series`, `aggregates`, `top_products`, `chart`.
   - График передавать дальше как base64 (решить, нужно ли в Telegram или только AI).

3. **Промпт**
   - Обновить `LLM_ANALYSIS_TEMPLATE.md`:
     - Добавить инструкции по интерпретации `time_series`.
     - Включить новые метрики (buyout/return rate).
     - Уточнить, что названия товаров должны браться из `name` или `nm_id`.

4. **Telegram вывод**
   - Отправлять график пользователю:  
     - Раскодировать base64 → отправить фото (как `send_photo`) перед текстовыми чанками.  
     - Учесть ParseMode=None, как и раньше.

## 🔄 Обновление документации

- `docs/stages/S5/S5.md`: добавить ссылку на данный ТЗ как подзадачу этапа 5.
- `docs/project/SPEC_TECH.md`: дополнить описание интеграции AI-аналитики новым эндпоинтом (после реализации).

## 🗓️ Роадмап реализации

1. **Backend**
   - Создать Pydantic-схемы (`DailyTrendsAPIResponse`, `DailyTrendPoint`, `DailyTrendAggregates`, `DailyTrendTopProduct`, `ChartPayload`).
   - Реализовать сервисный метод `BotAPIService.get_daily_trends` с кешированием.
   - Добавить эндпоинт в `routes.py`.
   - Написать unit-тесты.

2. **GPT-сервис**
   - Обновить fetch/aggregate/pipeline.
   - Расширить шаблон промпта.
   - Добавить отправку графика и адаптировать шаблон для нового текста.
   - Написать тесты на парсинг ответа (`validation`).

3. **Верификация**
   - Ручной вызов эндпоинта (curl/Postman) для проверки JSON и base64.
   - Прогон AI-анализа в dev-окружении: убедиться, что приходит график и корректный текст.

## ✅ Критерии приёмки

- Эндпоинт `GET /api/v1/bot/analytics/daily-trends` возвращает корректный JSON, содержит ровно `N` точек временного ряда, агрегаты и топ-товары с правильными названиями.
- График строится и приходит в ответе (проверка декодированного PNG).
- AI-аналитика использует новый эндпоинт и формирует текст с учётом новых данных.
- Telegram-бот доставляет пользователю изображение + текст отчёта.
- Покрытие тестами бэкенд-сервиса и GPT-сервиса ≥ 80% для новых модулей.


